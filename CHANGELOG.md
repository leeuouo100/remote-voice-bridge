# 更新日志

本文件记**每个版本改了什么、为什么改**。装哪个版本、怎么用，看 [README.md](README.md)。

版本号规则：`主.次.修`；tag 必须与 `config.py` 的 `APP_VERSION` 一致
（CI 会校验，不一致直接构建失败）。

---

## v1.0.5：把"要用户描述现象"改成"让程序自己测出来"

v1.0.4 修掉了主线故障，剩下一批小缺陷和两个"必须真机才知道"的疑问。
本版把它们一起收掉。

### 一、同类缺陷扫尾（v1.0.4 只修了一处，这里补齐）

| 位置 | 问题 | 修法 |
|---|---|---|
| `console_server._resolve_out_device` | 注释写着"子串命中优先取最短的"，**代码其实返回第一个命中的**。装了 VB-CABLE + CABLE 2 时下拉框显示的和实际用的不是一只声卡 | 改成真的三级匹配（全名 → 前缀 → 子串）+ 同级取最短名 |
| `mixer.find_input_device` | 子串匹配同样是"谁先枚举到用谁"。同一只声卡在 MME / DirectSound / WASAPI / WDM-KS 下各出现一次，MME 还会把名字截断到 31 字符，枚举顺序由驱动决定 | 同上，三级匹配 + 取最短名 |
| `config.Config.load()` | keymap 里非字符串的值被**静默丢弃**，那个按键永远没反应、日志一个字都没有 | 打印告警，列出被丢掉的条目 |
| `main._on_key` 里的 `last_ble_activity` | 注释写成"遥控器按键 = 它还活着"，但低级钩子**分不出来源设备**，敲物理键盘同样会刷新它 | 注释改成实话，并写明这是**刻意接受的保守偏差**（宁可偶尔漏判失联，也不要因为你敲键盘就误判掉线、把遥控器哑掉 4 秒） |
| `tools/check_injection.py` | 「卡键」判定没有排除"本轮开始前就已经按着的键"，上一轮残留会被算成这一次没释放，报出假 FAIL | 减掉本轮开始前就已按下的键；重试轮次 3 → 6 |
| `main._send_tx` | 投递失败时协程既没被接管也没 `close()`，解释器 GC 时甩一条 `coroutine was never awaited` RuntimeWarning，把真正的 `schedule failed` 日志淹没 | 失败分支先 `coro.close()` 再打错误日志 |

### 二、映射值写错不再静默失效

`send_key`遇到解析不出来的键名会**静默跳过** —— 用户看到"这按键没反应"，
日志里一个字都没有。手改过 `config.json`、或从旧版本带过来的配置很容易踩到。

现在开机（以及每次热重载）都会把用户实际在用的 keymap 校验一遍，
发现发不出去的键名就打 WARNING，并写清是哪个按键、哪个值、去哪里改：

```
⚠ 按键映射「home」→ 'win+ctr' 里有发不出去的键名 ['ctr']，这个键按下去不会有任何反应。
  请到控制台「按键映射」页改掉。
```

> `check_keymap.py` 只在 CI 里校验**默认表**，管不到用户实际在用的那一份。
> 这里补的是运行时的那半边。同一个坏值只吵一次，不会刷屏。

### 三、新增：遥控器真机诊断（`diag-remote.bat`）

**目的：把"你描述现象 → 我猜原因"换成"程序测出来 → 直接看结论"。**

双击仓库根目录的 `diag-remote.bat`，按 7 段提示按键（约 90 秒），
产出 `%APPDATA%\remote-voice-bridge\remote-diag.txt`：

1. 遥控器每个键在 Windows 上**实际发出什么**（名称 + 硬件扫描码）
   → 直接回答「有些按键没作用」到底是"没发事件"还是"映射写错了"
2. 遥控器 OK 键 与 物理键盘 Enter 的 `(名称, 扫描码)` **是否相同**
   → 这一条是决定性的：相同则底层**无法**区分来源设备，
     只能靠"吞掉"拦截、代价是物理键也被吞；不同则可以实现
     只拦遥控器、完全不动物理键盘的精确方案

它的「报告生成器」是纯函数式的，所以 `check_all` 会用假数据把
两条分支（能区分 / 不能区分）都验一遍 —— 否则那段代码第一次运行就是在用户机器上。

### 四、`check_all` 不再被机器负载骗

`check_injection.py` 是唯一会真的注入按键、读系统实时状态的一关，也是唯一受负载影响的。
实测同一份代码连跑两次，一次报 2 项 FAIL、一次全过。

现在 `check_all` 跑它之前先等 2 秒（让前一步的进程退干净），
失败且原因是「钩子没收到」时整步重跑一次：
**噪声被滤掉，真回归仍然每次都会红。** 这不是"把测试调松了"。

---

## v1.0.4：修掉「松手就断流 / 转文字逐字卡顿」——MIC_OPEN 其实从未发出

> 🔴 这是 v1.0.3 的真机事故修复版。**v1.0.3 建议直接跳过，装这个。**

### 症状（真机）

1. 按下语音键能说话，但**一松手**，混音输出立刻变成「等待录音」，再也收不到声音
2. 即便勉强在跑，**语音转文字一个字一个字往外冒** —— 说了近 40 秒，文字慢慢爬
3. 静音键的「按住说话」完全没反应

### 根因：Windows 蓝牙回调线程里没有事件循环

Windows 的 BLE 通知回调（ATVV / HID）不是跑在主线程上，而是跑在 COM 的线程池线程上
——线程名长这样：`Dummy-3240`。这些线程**没有 asyncio 事件循环**。

而 v1.0.3 的 `_send_tx()` 是这么写的：

```python
asyncio.get_event_loop().create_task(_write())   # ❌ 在 Dummy 线程上直接抛异常
```

结果：**每一条 `MIC_OPEN` / `MIC_CLOSE` 都在投递阶段就失败**，日志里能看到：

```
MIC_OPEN schedule failed: There is no current event loop in thread 'Dummy-3240'
```

但紧接着又打了一句 `🎤 松手后自动重新开麦` —— **这是假成功**（只要命令"构造出来了"
就算成功，根本没管有没有发出去）。于是：

- 遥控器从来没收到过「我要常开麦」的指令 → 硬件只在你**按住的那一瞬**推流
- 松手 → `audio_stop` → 遥控器停止推流 → 混音显示「等待录音」
- 反复的 start/stop 把音频切得七零八落 → 识别端一直在等"这句话说完了吗" → 逐字往外冒

`session.py` 里还有 4 处同样的 `asyncio.get_event_loop()`（开麦/闭麦的延时定时器），
**一模一样的坑**，一并修掉。

### 修法

| 位置 | 原来 | 现在 |
|---|---|---|
| `main.py` 的 `_send_tx()` | `get_event_loop().create_task()` | `asyncio.run_coroutine_threadsafe(coro, _bridge_loop)` 回投主循环 |
| `main.py` 的 `_on_mic_open/close` | 无脑返回命令 | 发不出去就返回 `None`，不让 `mic_open_sent` 被置位 |
| `main.py` 的 `audio_stop` 分支 | 假成功日志 | 真没发出去就打 `❌ MIC_OPEN 未能发出` 的 ERROR |
| `session.py` 的 4 处定时器 | `asyncio.get_event_loop().call_later()` | `threading.Timer(daemon=True)` |

同时在 `run_bridge` 开头存一份主循环引用：

```python
_bridge_loop = asyncio.get_running_loop()
```

### 顺带修的

- `on_mic_open_result` 排新定时器前先取消旧的（原来会留一个僵尸定时器）
- `config.py` 新增 `VIRTUAL_TARGETS` 单一真源 —— `voice_ptt` 是虚拟目标不是键位组合，
  之前两个自检脚本各写死一份，导致 `check_keymap` 误报 FAIL（会挂 CI）
- `tools/smoke_console.py` 的默认触发键断言过期（默认已改成 `ctrl+win+shift`），同步更新

### 第二轮：外部代码审查中发现并确认的缺陷

拿到一份逐文件审查报告（8 条）。逐条读源码核实，**6 条成立、2 条站不住**，
成立的都修了：

#### 🔴 语音会话进行中不再做 GATT 读

CTL 特征既承载 `audio_start` / `audio_stop`，又被当成健康检查的读目标。
Windows 的 GATT 栈同一时刻只允许一个 ATT 事务，**读占着通道期间到达的通知会被压后甚至丢**。
一旦丢掉 `audio_stop`，`voice_active` 永远回不到 False → 下一次按语音键被当成
"第二次按下"直接收尾，用户看到的是「说着说着自己断了」。
触发条件极日常：按过任意键后 3 秒内就会来一次读（`key_check_window`）。

现在 `check_health()` 在 `streaming=True` 时直接跳过读、只查连接状态。
语音期间本来就有音频帧按 ~50Hz 刷新 `last_ble_activity`，这个读纯属多余。
`force=True`（watchdog 判定静默超时）时照读不误。

#### 🔴 确认键吞 Enter 的行为改为可关闭

`_on_key` 里 `return False` 会吞掉「语音会话进行中按下的确认键」。
但 Windows 低级键盘钩子**分不出遥控器和物理键盘**，所以物理键盘的 Enter 也被一起吞
—— 会话没结束前键盘回车是死的。

同时核实了 `keyboard` 库的真实机制（读源码，不是猜）：
`hook(cb, suppress=False)` 把回调挂进 `handlers`，而 `direct_callback` 只检查
`blocking_hooks` —— **suppress=False 时 `return False` 是空操作，一个键都拦不住**。
这就是"不开启拦截遥控器原生按键就会输入失败"的真正原因。

因为设备层无法区分，只能给逃生口：新增配置
`swallow_ok_during_voice`（默认 `true` = 维持原行为），控制台「设置 → 语音」可勾选。
关掉后确认键仍会结束语音会话，只是额外多打一个回车。

#### 🟠 `_find_cable` 多虚拟声卡时选错设备

原来是"第一个子串匹配"。装了 VB-CABLE + CABLE 2 + Voicemeeter 时，
`CABLE Input` 是 `CABLE 2 Input` 的子串，而 PortAudio 的设备顺序由驱动枚举决定，
可能挑中 `CABLE 2 Input` —— 微信那边选的却是 `CABLE Output`，表现为
「一点声音都没有」，日志上完全看不出选错了设备。

改为：全名相等 → 前缀 → 子串，三级优先。

#### 🟠 `_mute_loop` 异常时 BACKSPACE 可能卡在按下状态

原来 down/up 一起包在 try 里、异常直接 `pass`。若 `send_key_up` 抛错
（Interception 驱动被拔、权限被收回），异常被吞、循环继续 `send_key_down`；
用户在此时松手，循环会在"已按下未释放"的状态下退出 ——
之后每碰一下键盘都在连续删除。

改为：释放失败立刻 `break`，并在 `finally` 里补一次幂等释放。

#### 🟡 `tray_app._bridge_worker()` 丢掉 traceback

只 `print(f"[bridge] {e}")`，现场只剩一行光秃秃的消息，不知道哪一行炸的。
改为补 `traceback.print_exc()` —— 桥会自动重连，这条记录是事后唯一线索。

#### 🟡 v0.4 帧长不符时静默丢帧

`len(data) != caps.frame_size` 直接 `return None`，日志上看不出有帧被吞。
补了一行 `logger.debug`。

> ⚠ **刻意没有**采纳报告建议的 `abs(len - frame_size) <= 1` 容差：
> v0.4 每帧自带预测器/步长（`data[3:5]` / `data[5]`），长度对不上就是协议对不上，
> 硬解只会把噪声当语音喂给输入法。宁可丢帧也不要喂错。

#### 核实后判定**不成立**的两条

| 报告 | 实际 |
|---|---|
| `last_key_activity` 只在下键刷新，长按会误判掉线 | Windows 的按键自动重复本身就会持续产生 `down` 事件，一直刷新。且 watchdog 只多一次读，无实际影响 |
| `Config.load()` 缺配置迁移提示 | 本版已经加了 `CONFIG_VERSION` + `_migrate()`（见上节）。报告猜的"keymap 用数字 ID"从未存在过 |

### 验证

模拟一个 `Dummy-7777` 线程跑完整链路（能力协商 → 开麦 → 收流 → 闭麦 → 松手后补开麦）：

```
正常路径：sent == ['open', 'open']            ✅ PASS
故障路径（主循环已关）：sent == []，
  mic_open_sent 保持 False → main.py 打 ERROR  ✅ PASS
```

已固化为 `tools/check_ble_callback_thread.py`，随 `check_all.py` 进 CI。

---

## v1.0.3：语音键「按一下长输」+ 静音键「按住说话」+ 呆瓜化默认

### 背景：两种输入法语音方式，全部走输入法**原生**能力

微信输入法自己的「设置 → 语音输入」面板上就写着两种方式。本版不再自己造轮子，
而是**原样适配**这两种，并分别绑到遥控器的两个键上：

| 遥控器键位 | 转发给输入法的键 | 输入法侧的模式 | 行为 |
|---|---|---|---|
| **语音键**（麦克风图标） | 左Ctrl + 左Win + 左Shift | **启动语音输入**（切换式） | 按一下**开始**，再按一下（或按确认键）**结束** |
| **静音键** | Ctrl + Win | **按住说话**（PTT） | 按住说，松手即结束 |

> 设计原则：**输入法负责"怎么说话"，本程序负责"这段语音会话什么时候开始、什么时候结束"。**
> 状态机必须留在程序里 —— 因为只有程序知道「**松开遥控器按键 ≠ 语音说完了**」，
> 输入法自己是判断不了的。（这一条是开发中走过弯路后定下来的：一度把状态全甩给输入法，
> 结果是"松手就断"；也一度想自己合成"一直按住 Ctrl+Win"的假长按，那是重复造轮子。）

### 一、语音键：按一下就一直听着，松手不结束

v1.0.2 及以前只有「按住说话」，按住不放很累；设置里虽然有
「按一下开始，再按一下结束」这个选项，但**实际不生效**。两个根因：

| # | 根因 | 后果 | 修复 |
|---|---|---|---|
| 1 | `audio_stop`（遥控器上报"松手"）**无条件**调用了 `voice_hotkey_up()` | 一次按下 = 立刻发"开始"又发"结束"，自己把自己取消掉 → 点按模式永远不生效 | 松手**不再结束**语音会话；只有"程序认定的结束信号"才结束 |
| 2 | 遥控器只在按住期间推流，松手后 `stream_active=False`，音频帧被静默丢弃 | 就算输入法还在听，喂进去的也是静音 | 松手后自动补发 `MIC_OPEN` 并恢复 `stream_active`，**麦克风继续收音** |

结束语音会话的三种方式（任一即可）：

- 再按一次**语音键**
- 按**确认键**（OK / 中间那颗）
- 超过 **10 分钟**未结束 → 自动收尾（防止忘关导致一直占用麦克风）

> **防自激**：自动重开麦后，遥控器往往会回一个 `audio_start`。
> 这个信号**绝不能**被当成"用户第二次按下"（否则立刻又结束了）。
> 现在有 1.5 秒的宽限期，宽限期内的 `audio_start` 直接忽略。

### 二、静音键：按住说话（PTT）

新增映射目标 **`voice_ptt`**，「按键映射」页里给静音键选它即可，
对应组合键在 `config.voice_ptt_keys`（默认 `ctrl+win`）。
按下=发组合键按下，松手=发抬起，纯 PTT，不参与状态机。

### 三、呆瓜化：装上就用，不用做任何选择

不再让人一项项选。运行一次：

```bat
python tools\apply_voice_mode.py           :: 应用推荐配置（默认）
python tools\apply_voice_mode.py --show    :: 只看当前配置
python tools\apply_voice_mode.py --hold    :: 退回"全程按住说话"
```

推荐配置 = 输入法选**微信输入法**、语音触发方式选**按一下开始/再按一下结束**、
清空自定义触发键（用输入法原生键）、按键映射恢复默认（静音键=按住说话）、
开启系统麦克风参与混音、开启按键映射。**新装的默认配置就已经是这样了。**

### 四、按键匹配不再误伤（v1.0.2 留下的 bug）

HID 上报的按键名是"长名字"，旧匹配用的是**子串**匹配，导致物理键盘的键被遥控器规则误抓：

| 物理按键 | 旧行为 | 原因 |
|---|---|---|
| `Page Up` | 被当成**方向上键** | `"up"` 是 `"page up"` 的子串 |
| `Left Windows` | 被当成**方向左键** | `"left"` 是 `"left windows"` 的子串 |
| `Backspace` | 触发 **Esc** | `"back"` 是 `"backspace"` 的子串 |

现在：先查**精确匹配表**，兜底只对**多词键名**（如 `page up`、`left windows`）做
**词边界**匹配，单词键名不再参与兜底。回归用例 18 条，0 失败。

### 五、参考上游 vRemoter v1.1.1，作废一条旧结论

上游 [CHANGELOG](https://github.com/VincentKingHsu/vRemoter/blob/main/CHANGELOG.md)
明确写着：官方 Chromecast 遥控器支持**短按切换 + 长按说话**，且
"短按后遥控器麦克风却未持续收音"被列为**待修的 bug**。

> ❌ **作废本项目此前的一条错误结论**："遥控器麦克风只在按住时推流，软件改不了"。
> 那不是硬件限制，是**本程序漏发 `MIC_OPEN`** 造成的，本版已修。

**已知遗留（暂不解决）**：vRemoter 在 macOS 上能做到"只拦遥控器的键、放过物理键盘同名键"，
因为可以区分输入设备来源。Windows 的 `WH_KEYBOARD_LL` **拿不到设备来源**，
做不到按设备独占拦截 —— 「设置 → 拦截遥控器原生按键」打开后，物理键盘的同名键也会被一起吞掉。
要做到完全隔离需要装 Interception 驱动，与"开箱即用"冲突，暂缓。

### 六、工程防呆

- 新增 `config.VIRTUAL_TARGETS`：把"禁用 / 原样直通 / 语音键 / 按住说话"这类
  **不是组合键**的虚拟目标集中登记，自检脚本统一跳过。
  此前 `voice_ptt` 没登记，`check_keymap.py` 把它当组合键解析并报"键名无法解析" —— CI 会挂
- `tools/smoke_console.py` 的默认触发键断言同步更新为 `ctrl+win+shift`
  （= 微信输入法「启动语音输入」，默认档），并加了一条默认 `hotkey_mode == tap` 的断言
- 修正 `VOICE_HOTKEY_PRESETS` 里 `Ctrl+Win+Shift` 那档的说明：
  它原先写着"微信 PC 客户端"，但现在**默认就是它**（微信输入法的启动语音输入）

---

## v1.0.2：补齐界面（此前是「盲操」）+ 修掉持续 0 帧

v1.0.1 装到 HP 笔记本上以后暴露了两件事：**语音依旧一帧都收不到**，
以及**整个程序没有界面** —— 只能在托盘右键里点来点去，按键映射只能改 `config.json`
文件，等于盲操。本版针对这两点。

### 键位更正（重要）：微信输入法的「按住说话」是 `Ctrl+Win`

打开微信输入法自己的「设置 → 语音输入」，面板上写着两条，**和本仓库早前的说法完全相反**：

| 面板上的条目 | 键位 | 说明 |
|---|---|---|
| **按住说话** | **Ctrl + Win** | 按住可语音输入，松手结束 ← **本程序要的就是这条** |
| 启动语音输入 | 左Win + 左Ctrl + 左Shift | 按下即开始，按任意键结束（切换模式） |

所以：
- 默认触发键已经回到 **`ctrl+win`**（不是 `ralt`）。
- 早前"微信输入法默认长按右 Alt"是**错**的；**右 Alt 是豆包输入法的键**。
- 早前"`Ctrl+Win` 只在微信窗口内有效"也是**错**的（微信 PC 客户端那条同样是系统级）。
- **但不要再把键位当真理写死**：面板里是可以改的。拿不准就看面板，
  或跑 `python tools\test_voice_hotkey.py ctrl win` 实测一次。

> 讽刺的是：微信输入法这条 `Ctrl+Win`，正好撞上下一节那个「Win 会被系统改写成 `0xFC`」
> 的坑 —— 也就是说**修正键位之后，那个坑才真正变成必经之路**。
> 好在 `order_press()` 已经把 Win 排在最先按下，实测钩子能收到货真价实的 `vk=0x5B`。

### 一、持续 0 帧的真正原因：MIC_OPEN 从来没发出去

日志里一直是 `▶ Audio START` + `本次共收到 0 个音频帧`。对照上游客端源码后确认：

| | Chromecast 遥控器实际行为 | 本程序原来的假设 |
|---|---|---|
| 按语音键时上报的 ATVV 控制事件 | 只有 `AUDIO_START (0x04)` | 期待 `START_SEARCH (0x08)` |
| 结果 | 开麦命令 `MIC_OPEN` **一次都没发出去** | —— |

`MIC_OPEN` 原本只挂在 `start_search` 分支上，而这个遥控器**从不发** `start_search`
（上游客端 `BLEBridge.swift` 里也是同一个现象）。遥控器必须先收到 `MIC_OPEN`
才会真正上传麦克风数据 —— 所以它一帧都不推。

**修复**：`AUDIO_START` 到达时补发一次 `MIC_OPEN`（`SessionCoordinator.ensure_mic_open()`），
幂等、不重复开麦；并修正了 `mic_open_sent` 标记的生命周期 —— 它原先在会话关闭时
不复位，会导致**只有第一次能用、之后每次都 0 帧**。

> 顺带修掉一个潜伏的自激隐患：遥控器走 HID 键盘通道，与物理键盘在 Windows 上
> **无法区分**，键盘钩子既能看见用户按的键、也能看见程序自己注入的键。
> 映射目标一旦落回同一个键名（确认键 → Enter 就是典型）就会无限递归注入。
> 现在注入时会登记"这个键是我发的"，钩子识别到回声直接丢弃。

### 二、补上控制台与按键映射界面

对标 [vRemoter](https://github.com/VincentKingHsu/vRemoter) 的控制台：

- **音频页** —— **三路实时波形 + 分段电平表**（电脑麦克风 / 遥控器麦克风 / 混合输出，
  每路可静音、独奏、开关是否参与混合）、**增益滑块**（拖完立即生效，不用重启）、
  **混合输出设备下拉框**（自动列出所有输出设备，VB-CABLE 排最前）、**电脑麦克风选择**、
  输入法 / 触发方式（按住 / 点按）、**「测试语音键」按钮**（模拟长按 1 秒，
  直接验证输入法认不认这组键）
- **按键映射页** —— 每个遥控器按键一行，下拉框挑目标动作。目标集对齐 vRemoter 的
  `RemoteMappingTarget` 并换成 Windows 语义：方向上/下/左/右、回车、Esc、退格、
  Delete、Tab、空格、PgUp/PgDn、Home/End、系统音量±、静音、播放暂停、
  显示桌面、搜索、截图、任务管理器、复制/粘贴/撤销/全选…… 也可以
  **「录制任意按键…」** 自己按一个组合键进去。改动**即时保存、无需重启**。
- **设置页** —— 上半：音频（两路增益、混合输出设备、电脑麦克风）与语音
  （触发方式、触发键、拦截开关、开机启动）；下半：连接与关于（版本、配置目录）
- **日志页** —— 实时日志

> **关于「原样直通」**：遥控器走 HID 键盘通道，它自己的按键 Windows 本来就收得到。
> 所以映射目标里有一项 **`原样直通`**（默认用于方向键）＝ 不做任何额外动作。
> 改成其它动作时会「原生键 + 映射键」一起发出 —— Windows 无法单独拦掉遥控器原生键，
> 要完全接管需在「设置」页打开「拦截遥控器原生按键」（代价：物理键盘的同名键也会被吞）。

### 三、工程防呆

- 版本号在 `config.py` 与 `installer.iss` 两处，新增 `tools/check_version.py` 做一致性校验；
  CI 打 tag 时也会校验 tag 与 `APP_VERSION` 是否一致，不一致直接**构建失败**，
  避免"tag 是 1.0.3、装出来还写着 1.0.2"这种只能靠重新发版来修的事故
- 新增 `tools/smoke_console.py`：离屏把控制台真的建出来跑几轮刷新，
  控件名写错 / 变量漏定义这类问题在 CI 阶段就被拦下，而不是等用户点开才炸
- 新增 `tools/check_injection.py`：**真的把组合键按下去**，再用键盘钩子确认系统收到了。
  这是唯一能拦住「SendInput 静默失效」和「组合键顺序错」的一关 ——
  这两种故障的共同点是**日志里一切正常、输入法毫无反应**，肉眼查不出来。
  下面那个 Win 键的坑就是它抓出来的
- `tools/check_all.py` 一键跑完全部校验，输出 `ALL CHECKS PASSED` 即通过

#### 顺带查清的一个系统级坑：注入 `Ctrl+Win` 时 Win 会被换成 `0xFC`

排查「按住 `Ctrl+Win` 唤不起输入法」时发现，问题不在我们发得对不对，
而在**系统收到之后把内容改了**：

| 注入顺序 | 键盘钩子实际看到的事件 | 结果 |
|---|---|---|
| 先 Ctrl 再 Win | `vk=0xA2`、**`vk=0xFC`**（Win 被顶替掉了） | 输入法认不出 Win，组合键失效 |
| **先 Win 再 Ctrl**（现在的做法） | **`vk=0x5B` / `scan=0x5B`**、`vk=0xA2` | 钩子收到货真价实的 Win ✅ |

原因是 `Ctrl+Win` 属于 Windows 保留的**系统组合键**：Ctrl 已经按下时，
系统会把后到的 Win 改写成 `0xFC`，而且 Ctrl 与 Win **不会同时**出现在
`GetAsyncKeyState` 里。所以现在 `keys.py` 的按下顺序固定为 **Win 最先落下**
（`order_press()` 按修饰键优先级排序），`check_injection.py` 对带 Win 的组合
只认钩子事件、不拿 `GetAsyncKeyState` 当判据。

> 结论：微信输入法要的正是 `Ctrl+Win`，而它恰好是**最受系统限制**的那一组。
> 我们已经把合成按键能做到的做到最好（Win 最先落下），钩子确认收到真实 Win；
> 输入法到底认不认，请按 [使用教程](docs/使用教程.md) 第六步实测一次
> （控制台「音频」页也有「测试语音键」按钮）。
> 万一还是唤不起来，就把输入法的键位也改成**右 Alt**（一路不涉及 Win，最干净）。

---

---

## v1.0.1：真机实测抓到的四个问题

v1.0.0 发布后在真机上跑，**遥控器这一侧完全正常**（配对、ATVV 握手、MIC_OPEN、
遥控器推流全都对），但**按着说话输入法一个字都不出**。日志把范围锁死到了音频之后：

> ⚠️ **最容易搞混的一点：微信输入法 ≠ 微信 PC 客户端。**
>
> | 产品 | 按住说话的键 | 生效范围 |
> |---|---|---|
> | **微信输入法**（本程序对接的） | **Ctrl + Win** | **全局**——微信、豆包、记事本、浏览器都能用 |
> | **豆包输入法** | **右 Alt** | 全局 |
> | 微信 PC 客户端 4.1.8+ | `Ctrl+Win`（持续输入 `Ctrl+Win+Shift`） | 系统级，**不只**微信窗口内 |
>
> 以输入法自己的「设置 → 语音输入」面板为准，别抄文档。
>
> 🔴 **本节早前写的「微信输入法 = 右 Alt」和「Ctrl+Win 只在微信窗口内」两条都是错的**，
> 已在 v1.0.2 按输入法面板实测更正 —— 见下文「键位更正」。

| # | 问题 | 后果 | 修复 |
|---|------|------|------|
| 1 | 把「按住说话」做成了「点一下」，且键位写的是 `Alt+Shift+M` | 输入法的语音输入都是**长按说话、松开结束识别**，点一下只录到约 50ms 空气；`Alt+Shift` 还是 Windows 切换输入法的系统热键 | `keys.py` 改用 **SendInput** 的 key-down/key-up，语音开始=按住、结束=松开；`hotkey_mode` 支持 `hold`/`tap` |
| 2 | **每个采样被播放两遍**：`on_audio` 既直接塞进播放缓冲、又入队，而播放回调两条路都会取到同一批数据 | 音频变成断续、倍速的碎片，就算触发了输入法，喂给 ASR 的也是垃圾 | 删掉直接 extend，音频只走队列一条路 |
| 3 | watchdog 每 **180 秒**无条件重连：它以为「ATVV 静默 = 掉线」，但**遥控器按键走 HID 通道、不产生 ATVV 通知** | 每次重连约 4 秒内遥控器完全不可用 | ATVV 静默只当「疑似」，必须再做一次 GATT 读确认失败才断开；HID 按键也刷新活动时间 |
| 4 | 默认触发键一度被改成 `ralt`（右 Alt） | 微信输入法「按住说话」绑的其实是 **Ctrl+Win**，配成右 Alt 连输入法都唤不起来 —— 右 Alt 是**豆包输入法**的键 | 键位回到 `ctrl+win`（v1.0.2 按输入法面板再次更正，见下）。这一轮留下的**真正有用的机制经验**是：修饰键必须用 `KEYEVENTF_SCANCODE` 下发**硬件扫描码**，否则左右分体的键会被当成左边那只；而 Win 组合另有一条系统限制，见 v1.0.2 那一节 |

同时补齐了**音频可见性**（此前完全没有）：控制台现在有
**实时语音波形 + 电平 + 本次帧数/峰值 + 「最后音频 N 秒前」**，
`Audio STOP` 时日志会写明本次收到多少帧。

> 这一整套的意义：把「没收到音频」和「收到了但没触发输入法」**一刀切开**，
> 不用再靠猜。控制台里出现「⚠ 本次 0 帧」就是前者。

---
