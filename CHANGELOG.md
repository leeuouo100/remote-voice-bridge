# 更新日志

本文件记**每个版本改了什么、为什么改**。装哪个版本、怎么用，看 [README.md](README.md)。

版本号规则：`主.次.修`；tag 必须与 `config.py` 的 `APP_VERSION` 一致
（CI 会校验，不一致直接构建失败）。

---

## v1.0.14：修回 v1.0.13 把语音输入改坏 —— 顺序错了，热键一次都没注入

> ⚠ **装过 v1.0.13 的人必须升到这一版** —— v1.0.13 的语音输入是 100% 不可用
> （不是偶发），且没有任何设置能绕开。装 1.0.14 覆盖安装即可，配置不用动
> （`CONFIG_VERSION` 仍是 3，本版只改行为、不改配置结构）。

2026-09-22 深夜，武哥装完 v1.0.13 之后：

> 「怎么又有问题，连语音输入都用不了，」

### 一、现场：连按 10 次，一次都没启动

真机日志（`23:34:36~47`）里只有这一个循环，1.3 秒一轮，重复 10 遍：

```
23:34:36,694 ▶ Audio START
23:34:36,695 📤 MIC_OPEN 补发（AUDIO_START 触发…）   ← ensure_mic_open() 成功，顺手记 mic_reopen_at = 现在
23:34:36,712 ▶ Audio START                            ← +17ms（遥控器收到 MIC_OPEN 的回响）
23:34:37,982 ⏹ Audio STOP （本次共收到 17 个音频帧，峰值 356）
23:34:38,072 ▶ Audio START
23:34:38,073 📤 MIC_OPEN 补发                          ← 又补发、又刷新窗口
        …（重复到 23:34:47）
```

**一行 `🎤 voice hotkey TAP` 都没有。** 热键一次没注入 → 输入法从头到尾没被叫起来。
波形还在跳（那 17 帧），所以这是**静默失效**：不崩、不报错、没有任何提示。

### 二、根因：补开麦与判回声的顺序反了

`ensure_mic_open()` 成功发出 MIC_OPEN 时，调用方**顺手**记 `mic_reopen_at = time.time()`。
v1.0.13 把这段补发放**在判回声之前**（且无条件、每次 `audio_start` 都发）：

```python
if session.ensure_mic_open():
    mic_reopen_at = time.time()          # ← 时刻写在这儿
if mic_reopen_at and (time.time() - mic_reopen_at) < MIC_REOPEN_GRACE:
    ...吞掉                              # ← 拿刚写下的时刻去比 ⇒ 必然 < 1.5s
```

⇒ **这一次（真按键）自己把自己判成了回声**、当场吞掉。

光吞一次还不至于全废，致命的是**它每次都会重来**：松手（`audio_stop`）会把相位打回
`CLOSED`、`mic_open_sent` 复位（`session.py:193`）→ **下一次按键**又补发一次、
又刷新一次窗口 → 每一次按键都落在窗口内 → **一次都启动不了**。

### 三、⚠ 顺带纠正一个我原先判错的版本结论

我一开始以为"顺序问题"是 v1.0.13 引进的。**查了 v1.0.10/11/12 才发现三版都有这个顺序**，
它们之所以"能用"，是**靠回响顶上**：

```
22:43:49,915 ▶ Audio START        ← 真按键
22:43:49,915 📤 MIC_OPEN 补发      ← 记下 mic_reopen_at
22:43:49,935 ▶ Audio START        ← +20ms 回响
22:43:49,936 🎤 voice hotkey TAP  ← 热键是**回响那一下**触发的
```

真按键被吞掉 → 由于旧版**吞掉时会把 `mic_reopen_at` 清零** → 紧接着的回响落进
`elif not voice_active` → 把会话真正启动起来。**清零点正是旧版能跑起来的原因**，
而且时序一歪（第 3 个回响）就会「按一下、刚开口就被关掉」（那 4/10 次的病）。

⇒ v1.0.13 把这个清零去掉（方向对），但没动顺序，于是从"偶尔被关掉"变成"永远起不来"。

### 四、修法

| 改动 | 从 | 到 | 为什么 |
|---|---|---|---|
| **顺序** | 先补开麦、后判回声 | **先判回声、后补开麦** | 补开麦顺手记的时刻绝不能参与这一次的判定 |
| 补开麦的落点 | `audio_start` 分支顶部、无条件 | **`elif not voice_active`（真按键开始新一段）里** | 开麦只在一段开始时需要；挂在顶部就是每次都给自己造窗口 |
| 窗口宽度 | `MIC_REOPEN_GRACE = 1.5` | **`0.8`** | 实测回响 +17/+90/+300/+320ms（≤350ms），真按键 ≥1s；0.8 两边都留余量 |
| 窗口的维持 | 吞掉时清零 / 刷新 | **只靠时间过期，任何事件都不许改它** | 清零＝只挡一次（会话被自己关掉）；刷新＝永不过期（按键全被吞） |

修好后的时序（同一个按键）：真按键 → 判回声=假 → 开始 + 注入热键 → 补发 MIC_OPEN
→ 回响（+17ms）被吞 → 松手继续收音 → 再按一下（≥1s）→ 结束。
**热键由真按键触发，回响彻底不参与状态机。**

### 五、闸门加强（`tools/check_voice_session.py`）

新增 **3 项断言 + 1 条反例**（原 2 条 → **3 条反例**）：

- **①' 顺序铁律**：`audio_start` 分支里 `ensure_mic_open()` 的位置必须**晚于**
  `is_echo =` 那一行；少了补开麦也要报红（否则"按键一帧都不推"）。
- **①' 窗口宽度**必须落在 `[0.5, 1.2]` 秒。
- **① 回声分支里不许出现任何 `mic_reopen_at =`**（清零与刷新是同一个坑的两个方向）。
- **④ 帧计数清零**也必须在判回声之后（否则回响那一下把本段帧数抹成 0，零帧误触复发）。
- 反例：把补开麦挪回判回声**之前** → 必须报红。

> 教训：**顺序耦合在源码里完全看不见** —— 那两行各自都合法、单独看都对、
> 换行位置也不报错。这次能查出来全靠"日志里少了一行"。所以它必须由闸门钉死，
> 不能靠人记得。

---

## 未发布（工具侧）：0x1812 这条路被 Windows 封死了 —— 按键到此为止

2026-09-22 夜里，武哥授权跑了最后一个纯软件实验（`tools\takeover_hid_reports.py --go`，
以管理员身份）：临时停掉遥控器那个 HID-over-GATT 节点，好让 `0x1812` 空出来自己订 Report。

### 一、结论：不是我们没测成，是 **Windows 不许任何人让它放手**

| 入口 | 结果 |
|---|---|
| `Disable-PnpDevice -InstanceId …` | ❌ `DISABLE_FAIL: 不支持`（= `ERROR_NOT_SUPPORTED`） |
| `pnputil /disable-device "…"` | ❌ `Cannot disable critical system device`（**rc 却是 0**） |

再只读查一下那个 devnode 的属性，原因一目了然：

```
DevNodeStatus = 0x0180000A   → DRIVER_LOADED | STARTED …
                 ↑ 没有 DN_DISABLEABLE(0x08000000) 这一位
Capabilities  = 0x80         → 只有 SURPRISEREMOVALOK
InfPath       = hidbthle.inf （Windows 的 HOGP 驱动包）
RemovalPolicy = 3            （EXPECT_ORDERLY_REMOVAL）
```

**没有 `DN_DISABLEABLE` ⇒ 设备管理器里「禁用设备」也是灰的 ⇒ 用户态没有任何办法。**
节点禁不掉 → HOGP 栈一直占着 `0x1812` → 我们自己订就永远是 `ACCESS_DENIED`
（实测正是如此，`特征枚举被拒：ACCESS_DENIED`）。

⇒ **「自己订阅 `0x1812` 读遥控器按键」这条纯软件的路，到此为止。**
再往下只剩内核过滤驱动或换硬件 —— 两条都不是本项目要走的（加硬件已被武哥否决）。
收尾回读 `STATUS=OK`（其实压根没被禁掉，设备没受影响）。

### 二、顺手发现的两件事

1. **这台机器上同一个遥控器配对过两次**：`find_all` 出来两个 `'Chromecast Remote'`，
   本地地址分别是 `04:7f:0e:f2:d2:94` 与 `04:7f:0e:90:11:01`（两块蓝牙）。
   旧代码「挑第一个名字像遥控器的」从没被验证过 —— 现在会把整张表打出来再逐个试。
2. **`pnputil` 失败也返回 `rc=0`**。只判返回码会把"没禁成"记成"禁成了"，
   再拿这个假阴性去下结论。所以判据一律看输出**正文**，不看 rc。

### 三、工具自身修掉的两个 bug（都是这次才暴露的）

- `Start-Process -ArgumentList` 是**用空格把参数拼成一行**的：路径里有空格
  （`D:\Pi Agent\…`）会被切断，python 一闪就退、什么都不打印 —— 于是"实验根本没跑"，
  而外面只看得到"什么都没发生"（三次 UAC 都白点）。改成**相对路径 + `-WorkingDirectory`**。
- GATT 那半换成"**全列 + 逐个 `from_id_async`**"（旧写法直接挑第一个，
  真机上吃 `E_INVALIDARG`，而且一个设备名都没打出来，无从排查）。

### 四、按键取证：**遥控器的键从来没进过 Windows**（把"没测成"和"没发"分开）

"禁不掉"只说明**我们读不到**，不等于"遥控器没发"。这两件事必须分开证。
于是回头把真机日志（`115302 → 115377` 行）整体过了一遍：

| 取证项 | 结果 |
|---|---|
| `🔘 HID 按键` 行 | **343 条**，逐条看**全是字母/数字/修饰键**（`s`/`g`/`a`/`b`/`backspace`/`v`/`l`/`w`/`x`…）＝**人在敲物理键盘** |
| 那批**负 scan 码**的（`f13`=-124、`left ctrl`=-162、`left shift`=-160、`reserved `=-252） | **热键注入的回环**（我们自己发的 `lctrl+lwin+lshift` 被全局钩子又报了一遍） |
| **遥控器独有的键**（`up`/`down`/`home`/`volume up\|down\|mute`） | **零出现** |
| `📊 HID 通道审计` | **13 条，全部"累计 0 条原始报告"** |
| ATVV CTL「未处理的控制指令」 | **0 条**（遥控器唯一的 CTL 输出就是 `0x04 audio_start`） |
| 遥控器在 `0x1812` 上声明了几个集合 | **5 个**（`0x01/0x06` 键盘、`0x0C/0x01` 消费类、`0x01/0x02` 鼠标、`0xFF01`/`0xFF80` 厂商），**一条报告都没来** |

**「那 3 路 GATT 读不到」只证明"没走那 3 路"，键盘集合那一路的反证靠上面那 343 条**——
遥控器若真从键盘集合发过键，`🔘 HID 按键` 里必然出现 `up`/`down` 这类遥控器独有键，
实际是零。两条合起来才闭合。

⚠ **一个真实踩过的坑（方法论）**：`2026-09-17 01:41` 的日志里曾有
`🔘 HID 按键 'left'（scan=75） → 按钮「left」→ 动作 'native'`、`'enter'（scan=28） → 按钮「ok」`，
**单看这两行像是"遥控器按键进去了"**。摊开上下文才发现同秒混着 `s/g/a/b/backspace/v/l/w/x`
—— 那是**用户在敲物理键盘**（正好按到方向键和回车）。
⇒ **单行日志不能归因**，要看上下文 + 键名分布 + scan 码符号。

### 五、"重新 bonding 绕开 HID 独占"—— **对本设备是 no-op**

另一条线主张"bleak 的 WinRT 后端实现了主动 bonding，纯软件有戏"。核实结果：

- **有 `pair()` 这个方法**，但对**已配对**的设备它什么都不做 ——
  `bleak/backends/winrt/client.py` 里第一句就是
  `if device_information.pairing.is_paired: return`。我们的遥控器**早就配对了**，
  所以调它 = 空转（要真重配得先 `unpair()`）。
- 更关键的是**分类错了**：`0x1812` 被 `ACCESS_DENIED` 不是因为"配对仪式没走对"，
  而是因为设备**已经被判定成 HID 设备**（`SERVICE=mshidumdf`、
  `DRIVER=Bluetooth Low Energy GATT compliant HID device`，
  子节点挂 `kbdhid`(Keyboard)/`mouhid`(Mouse)/3× HIDClass）
  —— 这是**服务归属**问题，跟谁执行配对无关。重新 bonding 一遍，归属照旧。
- ⚠ **护栏**：真去 `unpair()` 是**有风险**的 —— 配对记录**按 USB 口绑定**
  （见 v1.0.10），解绑后可能连不上、且软件侧恢复不了。**别试。**

### 六、留一个探针（以后任何"有戏"的主张，正面验证）

上面那张取证表是从**历史日志反推**的。为了让下一次不用再翻 11 万行，
新增 `tools/probe_remote_keys_reach.py`：**基线 10s + 取样 N 秒**，
三态结论（进了 Windows / 没进 Windows / 无法判定），
`--selftest` 四条自证（**含"只喂合成事件 → 必须仍判没进"**）。

⚠ 它是**现场探针**，要人在遥控器上按键，**不许加进 `check_all.py`**。
用法见 [tools/README.md](tools/README.md) 的「现场探针」一节。

---

## v1.0.13：说完话不用再够鼠标 —— 但「要不要发」由你决定

2026-09-17 武哥的原话：

> 「我说完话了，按完语音结束，就可以退出，然后按回车就把这个消息发送给你。
>   但是你现在没有把这个功能开发出来……我还要去电脑上操作鼠标去点发送。
>   这已经完全没有达到我要求的那种 voice coding 的感觉了。」

问题不在识别（识别一直好用），缺的是**闭环的最后一环**。

### 一、⚠ 中途被否掉的第一版设计（记录下来，别再走回去）

第一版做的是「按语音键结束 → 800ms 后**自动**替你按回车」，**默认开**。
武哥看完直接否掉：

> 「我希望说完之后，这个东西什么时候发送由我来决定。万一我写到一半要上厕所去了，
>   我回来再修改、再发送什么的，你没经过我同意就自动发送，把我没想好的说法给
>   发送过去了，那不是出问题了。」

**他是对的，这个默认值本身就是错的。** 自动发送会把**还没想好的话**替用户发出去 ——
而且发到真人对话里，**不可逆**。代价远大于"要自己动手发一下"。
第一版的错在于：把"省一次点击"当成了收益，却没算"替用户说出他还没决定要说的话"的风险。

**定案（v1.0.13 的最终行为）**：

| 行为 | 谁做主 |
|---|---|
| 说完 → 文字落进输入框 → **停在那儿** | 用户（想改就改） |
| 什么时候发 | **用户按键盘回车**（手本来就在键盘上；原来别扭的是"得去够鼠标"，那一下才打断思路） |
| 自动发送 | **默认关**。开关留着，但默认不替用户做主 |

配套把**兜底方向**也翻了过来：`cached.get("send_after_voice", False)` ——
读不到配置时必须按"不发"处理（原来写的 `True`，配置一残缺就会替用户发消息）。
`CONFIG_VERSION` 2 → 3，`_migrate` 把任何被打开过的自动发送**归位成关**。

> 一般规律：**越是"替用户说话/替用户做主"的自动化，默认值越要往保守那边放。**
> 「少做一次」用户可以补；「多做一次」往往补不回来。

### 二、为什么不能靠遥控器按键（这是原始难点）

遥控器除语音键以外的按键走 HID 厂商页，在 Windows 上至今收不到报告
（见 v1.0.12 的推翻结论）。所以「给遥控器上某个键绑一个回车」这条路当时走不通
—— 根本没有可用的键。

**哪天那条路通了，把某个键绑成「发送」才是最理想的解法**：既不用摸鼠标，
又完全由用户决定什么时候发。现在先用键盘回车顶着 —— 它已经去掉了"必须够鼠标"那一下。

### 三、做法

`config.py` 新增三项（**第一个默认是关**，另两个只在开关打开后才起作用）：

| 字段 | 默认 | 说明 |
|---|---|---|
| `send_after_voice` | **`false`** | 说完自动发送 —— **默认关**，什么时候发由你决定 |
| `send_after_voice_delay_ms` | `800` | 等输入法把识别结果落进输入框 |
| `send_after_voice_key` | `"enter"` | 发送用哪个键（键名同 `keys.py`） |

`main.py`：

- 语音会话结束时 `request_voice_send(本次帧数)` 登记一次待发送；
- **主循环**里 `maybe_send_after_voice()` 到点执行 —— 不走 BLE 回调线程：
  那边没有 asyncio 循环（v1.0.3 的坑），而且不方便读配置；
- 开关没开（默认）→ 登记立刻作废，**一个键都不按**；
- 开始新一段时 `cancel_voice_send()` —— 否则「说完觉得不对、接着说」
  会把上一条**残缺消息**发出去；
- **一帧音频都没有时不发送**：那多半是误触，而按回车会把输入框里
  **原有的内容**发出去，比不发送更糟；
- 读不到配置时按 **不发** 处理（`cached.get("send_after_voice", False)`）——
  兜底方向必须指向安全的那一边。

`console_server.py` + `ui/`：控制台「设置」页新增开关 + 发送键 + 延迟。
⚠ 三个字段**必须同时进 `_get_cfg` 的白名单和 `/api/config` 的 allowed** ——
本项目的老坑：漏了就是「界面上改了、后端当没听见」，而用户看到的只是"它不听话"。
⚠ 界面回填必须写 `=== true`，不能写 `!== false` —— 后者在字段缺失时会显示成
**已勾上**，与后端"缺字段＝不发"的兜底方向正好相反。

### 四、踩过的坑（含两个「写完了却永远不会触发」的真问题）

| 坑 | 现象 | 修法 |
|---|---|---|
| **`global` 晚于使用** | 在主循环里写 `global _pending_send_at` → 直接 **SyntaxError**：`name '_pending_send_at' is used prior to global declaration`（循环条件 `if _pending_send_at:` 先读了它） | 把状态改动收进模块级函数，主循环只负责调用 |
| **发送延迟不能小** | 太小会在输入法把文字落进输入框**之前**就回车 —— 等于把用户刚说的话弄丢一次，比不自动发送更糟 | 默认 800ms；控制台输入框强制 ≥ 300ms；闸里断言 ≥ 300ms |
| 🔴 **收尾时帧计数被自己清零**（功能写完却**永远不会触发**） | 帧计数原本写在 `audio_start` 分支**顶部**，而「第二次按下（＝收尾）」**也走 `audio_start`** → 先清零、再回头读它去登记发送 → 传进去**永远是 0** → 零帧误触保护每次都判定成「误触」→ **自动发送永不触发**。更糟的是日志会理直气壮地报「本次语音没有任何音频帧 → 判定为误触」，把排查方向指歪 | 帧计数只在「**开新一段**」分支里清零；闸里断言它**不在** `audio_start` 顶部、且**在**开始分支里。<br>老病复发：**「0 帧」和「误触」长得一模一样**，这次是程序自己把计数器清零造成的 |
| 🔴 **收尾路径不止一条** | 全项目有 **4 条**路径会把 `voice_active` 置 False：① 第二次按语音键 ② 键盘钩子的确认键 ③ 厂商页确认键 ④ 超时自动收尾。第一版只给 ① 接了自动发送 → 另外 3 条走完**静默不发** | 逐条按各自语义接上：<br>· ②只在 `swallow` 时补 —— 不吞那一下时 OK **自己就会变成 Enter**，再补一次就是连发两下（多发一条空消息）<br>· ③无条件补 —— 这一下被程序吞掉（直接 `return`，走不到 `resolve_button`）<br>· ④**故意不发送** —— 麦克风已开着最多 10 分钟，里面很可能全是环境音/旁人的话，自动发进聊天框比「少发一次」严重得多。<br>本项目铁律再现：**同一套机制在多条路径都要落地，漏一条等于没有** |

### 五、闸

`tools/check_send_after_voice.py`（新增：**27 项静态 + 9 项行为级 + 14 条反例自证**）。

**第一条行为级用例就是「默认配置下到点了也绝不发」** —— 它排在所有用例最前面，
默认值一旦回退成开，先红的就是它。这条用例本身就是把武哥那句话翻译成了断言。

**行为级那 9 项**才是真正说明问题的那一层：前面那个"永远不触发"的 bug 静态看完全正常。
做法是把 `main` 真的 import 进来（`APPDATA` 先指到临时目录 —— 否则 import 就会
**追加写**用户真实的 `bridge.log`），换成假时钟 + 假 `tap_key`（绝不真注入，
不然会往用户正在用的窗口里打字），然后真调 `maybe_send_after_voice`：
默认不发 / 延迟没到不发 / 到点发一次 / 发过不重复 / 零帧不发 / 取消后不发 /
关开关不发 / 换键真的换（`ctrl+enter`）/ 配置残缺不崩**且兜底不发**。

反例里有一条**一开始没报红**：检查写的是"这一段里出现过 `"send_after_voice"`"，
而同一行的 `getattr(new, "send_after_voice", True)` 让改坏后的代码照样蒙混过关。
收紧成必须匹配 `"字段":` 这个**白名单条目**的形态之后才报红。
**「看它出现过」不等于「它真的在那个 dict 里」。**

另一条反例也假绿过：构造"删掉厂商页确认键那条登记"时用的是正则
`日志行\n 调用`，而真实代码里两者之间**夹着解释注释** → 正则匹配不上 → 等于没改坏。
改成按**位置窗口**删（对注释免疫）才报红。
**用「精确形状」构造反例，等于给自己开了后门 —— 反例必须能改坏真实代码，而不是理想化的代码。**

另外把「各路 HID 集合到底收到过几条报告」做成 `remote_hid.py` 的定期审计日志
（`📊 HID 通道审计`，每 20 秒一条）。在这行出现之前，"按键没反应"在日志里是
**一片空白**，和"遥控器没连上"完全无法区分；有了它，按一次键就能定案：
计数在涨 = 报告来了（问题在解码/映射表），全 0 = 报告没来（问题在蓝牙/HID 层）。

⚠ 但审计行**自己也得限流**：桥是连着跑几天的，按键一直不来时 20 秒一条
＝一天 4300+ 行重复，把日志淹掉，真正有用的行反而找不到了
（"体检报告刷屏"，算是"静默丢弃"的镜像病）。规矩是
**前 3 次照报（刚启动排查时一眼就看得到），之后压到 5 分钟一条**；
有报告时也只在**计数变化**或过一个心跳周期时才打明细。
判决能力一分没少，噪音降两个数量级。
闸：这部分由 `tools/check_remote_hid.py` 用**假时钟真跑**验证（不是读源码）——
30 次审计（＝10 分钟）必须既"报过"又"没刷屏"，外加两条反例自证。

### 六、诊断工具的判读在骗人 —— 「自己注入的键」被当成了遥控器按键

起因是要回答"按键映射为什么一直做不成"。翻出 09-19 那次全通道监听的报告，
结论表长这样：

```
  ⌨ 键盘钩子（Windows 键盘事件）            33
  📦 HID — 0x0001/0006 键盘                0
  📦 HID — 0x000C/0001 消费类控制            0
  📦 HID — 0x0001/0002 鼠标                0
  📦 HID — 0xFF01/0001 厂商自定义            0
  📦 HID — 0xFF80/0000 厂商自定义            0
【判读】→ 键盘事件收到了：按键**能**到 Windows，问题在我们这层的映射/注入。
```

**五路集合全是 0，却下了"按键能到 Windows"的结论。** 那行判读是 `if kb_n:`
写出来的 —— 只要钩子还活着这个条件就**恒真**，跟遥控器有没有反应毫无关系。
那 33 个事件里绝大部分是**本程序自己注入的语音热键**（按一次语音键注入
`lctrl+lwin+lshift`，3~4 个事件），物理键盘敲的字也一并算在里面。

代价是实的：连着两轮排查都照着"问题在映射/注入层"去改，方向从一开始就是错的。

根因是 **`keyboard` 库的钩子只给键名、看不见 `LLKHF_INJECTED`** ——
"这个键到底是谁发的"在报告里根本没有地方表达。

| 改哪 | 改成什么 |
|---|---|
| `hidwatch.py` | 自带一个 `WH_KEYBOARD_LL` 低级钩子（纯 ctypes，不依赖 `keyboard` 包），把 `LLKHF_INJECTED` 一起记下来。`key_events` 变 5 元 `(ts, kind, name, scan, injected)`，`injected` 是**三态**：True / False / None |
| 判读 | **只有 `injected is False` 才能支撑"按键到了 Windows"**。注入事件单独一栏计数；分不出时明说"无法下结论"。以前那种恒真的写法不许再出现 |
| `watch_all_channels.py` | ① 键盘事件与通知原文**改走 `say()` 写进报告**（以前只 `print`，报告里只剩一个干巴巴的 "33"，连键名都没有，没法二次判读）② 判读**不再自己抄一份**，直接调 `hidwatch` ③ **「订上了但 0 条」的通道也进表**（`ae42` / `d343bfc5` 以前根本不出现，事后分不清"是 0"还是"没测"）④ GATT 那一半没订上时明说"私有服务这轮没测" |

⚠ 低级钩子装不上时会**静默降级**成"分不出是否注入"—— 那是最容易被忽略的失败态
（看起来照常输出）。所以报告里必须写清用的是哪种钩子、以及装不上的原因。

闸：`tools/check_hidinfo.py` 与 `tools/diag_remote.py` 的自检各加了两条 ——
① 有**真实**事件才准说"按键到了 Windows"；② 只有注入事件时**必须**说"一个都没到"。
反例是"把注入标志从判读里拿掉，上面两段判读必须变得一模一样"，拿掉就红
（这样它测的是"标志真的参与了判定"，而不是在看碰巧印出来的字）。

真机自检：注入一个 F24（无任何系统绑定），钩子必须记到它、且 `injected=True`。

> 顺手记一个坑：写 SendInput 测试时 `INPUT` 结构里的 union **必须包含
> `MOUSEINPUT`**。union 大小由最大的成员决定，只写 KEYBDINPUT（24B）再拿
> `c_byte*24` 凑数，x64 上 `sizeof(INPUT)` 会算成 28 而不是 40，
> `SendInput` 直接 `err=87`（ERROR_INVALID_PARAMETER）拒收，现象是"按键发不出去"。

### 七、`0x1812` 底下不是一个节点 —— 「禁错一个」本来会得出**反的**结论

武哥把硬件那条路直接否了：

> 「P32我是不打算去做的，因为又要加个硬件，就完全违背了我用这个遥控器做语音输入
>   或者是 Vibe Coding 遥控器的初衷了。」

（他说的 P32 就是 ESP32。那个方案的前提"软件层已经不可能"从来没被验证过，
`NullHidGatt.inf` 还是**未签名**的、注定装不上。）于是只剩最后一条纯软件的路：
把 Windows 那个 HOGP 节点临时停掉、让 `0x1812` 空出来，我们自己订阅 Report(`0x2A4D`),
**直接看遥控器到底发不发按键报告**。

写这个工具时差点栽在一个地方：`0x1812` 底下**不是一个节点，是一父 + 5 个 HID 子集合**。

```
BTHLEDEVICE\…\9&1DC2FEEB&2&0023   HIDClass  mshidumdf   ← 握着 ATT/GATT 会话的
HID\…&COL01\…                     Keyboard  kbdhid
HID\…&COL03\…                     Mouse     mouhid
HID\…&COL02/04/05\…               HIDClass  (无服务)
```

第一版写的是 `Get-PnpDevice | Where InstanceId -like '*00001812*' | Select-Object -First 1`.
`-First 1` 拿到的是枚举顺序里第一个，真机上实测是 **`COL05`**（一个无服务的空集合）。
禁掉它，会话还在父节点手里 → **一条报告也收不到** → 工具会理直气壮地判
"遥控器确实不发按键，软件层到头了"。

**那就是拿一个假阴性去关掉最后一条路。** 差一点。

| 改哪 | 改成什么 |
|---|---|
| `PS_FIND` | ① 把 6 个节点**全列出来**（免得再"测的不是你以为的那个"）② 目标**只认** `BTHLEDEVICE*`，找不到就输出 `TARGETKIND=NONE`，Python 侧直接 `return 3`，**不退而求其次** |
| 顺带修 | `$d.Service` 在 PS 5.1 的 `Get-PnpDevice` 对象上不一定有值（空得看不出来），改成显式取 `DEVPKEY_Device_Service`；读不到就印 `(读不到这个属性)`,不印空白 |
| 顺带修 | 中文 Windows 的 PowerShell 往管道写的是 **CP936**，Python 按 UTF-8 解全是乱码 → 每条命令前置 `[Console]::OutputEncoding=[Text.Encoding]::UTF8` |

判读是**二值**的，不许含糊：**收到报告** ⇒ 按键走 `0x1812`，本项目可以自己解码（纯软件就能做按键映射）；
**订上了但 0 条** ⇒ 遥控器确实不往 HID 送按键数据，软件层没有可做的事了（也**不会再买硬件去试**）。

安全设计三条（都写进闸里了）：默认只读（不带 `--go` 一个字节都不改）；
禁用与恢复在**同一条** PowerShell 命令里（禁用→睡→启用），Python 这边崩了/Ctrl+C 也照样恢复；
收尾**回读**节点状态，不是 `OK` 就喊人。

闸：`tools/check_takeover_guard.py`（含 2 条反例自证 —— 改回
`Get-PnpDevice | Select-Object -First 1` 必须报红）。

> 这道闸自己第一版也栽在同一个坑上：判据用字面量匹配，而 `BTHLEDEVICE` 和
> `Disable-PnpDevice` 恰好都写在**注释和说明文字**里 → 反例报不出红、只读分支被误判成"在禁用"。
> 加 `_ps_code()` / `_code_only()` 先剥掉注释与 `say(...)` 输出行再判。
> （`check_audio_watchdog.py` 的 `except queue.Full` 栽的是同一件事，已记在那边。）
>
> 一般规律：**判据会被"说明这段代码在干什么"的文字污染。** 注释里复述一遍老写法
> 是人类的好习惯，却是字面量判据的毒药。

### 八、⚠ 「按一下、刚开口，输入法就被关掉」——防自激只会挡**一次**

2026-09-22 晚上武哥的原话：

> 「刚才好久都没办法语音输入……只有遥控有声音，电脑麦克风没有声音。」

翻真机日志，22:00 之后的 10 次语音会话里，**4 次在 1.2~1.6 秒内被结束**。
一秒钟之内的三件事（22:44:53）：

```
22:44:53.515  ▶ Audio START          ← 第 1 个（我们补发 MIC_OPEN 的回响）
22:44:53.515  🎤 松手后自动重新开麦 → 遥控器麦克风继续收音
22:44:53.534  ▶ Audio START          ← 第 2 个，隔了 19ms
22:44:53.538  🎤 voice hotkey TAP    ← 又注入一次热键 = 把输入法语音输入**关掉**
22:44:53.625  🎙️ 语音会话【结束】（第二次按下语音键）
```

**用户按一下键、刚开口说第一个字，程序就把输入法关了 —— 一个字都出不来。**

根因就在"防自激"那三行：遥控器是 PTT 硬件，松手时会自己停推流，所以松手时
我们**补发 MIC_OPEN** 让它继续收音；它收到后会**回响**一个 `audio_start`
（那是我们要来的，不是用户又按了一次）。老写法是：

```python
if mic_reopen_at and (time.time() - mic_reopen_at) < MIC_REOPEN_GRACE:
    mic_reopen_at = 0.0      # ← 挡掉这一次，然后清零
    logger.debug("↩ 忽略自动重开麦触发的 audio_start")
elif not voice_active:
    ...开始
else:
    ...结束                  # ← 第 2 个回响落到这里
```

**"清零"意味着只挡一次。** 实测回响不止一个（19ms 内来两个）：第一个被吃掉并
清零之后，第二个就落进 `else` → 当成"用户第二次按下" → 注入热键 + 结束会话。

"清零"原本大概是想"别把用户真按的第二次也吞掉"。但代价方向搞反了：

| 出错方向 | 后果 |
|---|---|
| 少吞一个回响 | 会话被莫名其妙关掉、输入法被关 —— **用户当场用不了** |
| 多吞一个真按键 | 会话多听一会儿，再按一下就结束 —— **无害** |

按项目定案（拿不准就往"不做"那边倒）：**窗口内全部吞掉，不设次数上限**。
顺手把"吞掉"这件事从 `logger.debug` 提到 INFO 前 3 次 —— 这类"静默丢弃"
老毛病让这次排查时最关键的两个事件在正式版日志里根本不存在，只能靠时序反推。

闸：`tools/check_voice_session.py`（钉「会话不许自己把自己关掉」：窗口内要能吞下
**多个**回响、松手不许结束会话、吞掉的必须可见、帧计数位置。含 2 条反例自证）。

### 九、⚠ 「电脑麦克风」被配成了自己正在播的那根虚拟线

同一次日志里的另一件事：

```
✅ 电脑麦克风：CABLE Output (VB-Audio Virtual   44100Hz → 16000Hz
```

查系统默认录音设备：**就是 `CABLE Output (VB-Audio Virtual Cable)`** ——
因为接入说明让用户把默认录音设备设成它（输入法的语音输入要听遥控器的声音）。
而 `config.json` 里 `system_mic_device` 是空字符串 ＝"用系统默认"，于是
**混音里的「电脑麦克风」一路，读的是本程序自己刚播出去的虚拟线**：

```
本程序 ──写──▶ CABLE Input ──VB-CABLE──▶ CABLE Output ──读──▶ 「电脑麦克风」
   ▲                                                              │
   └──────────────────── 混进输出 ────────────────────────────────┘
```

两个后果：

1. 对着真麦克风说话，控制台「电脑麦克风」那一栏**永远不动**（武哥问的正是这个）。
   真麦克风 `麦克风阵列 (Realtek(R) Audio)` 从头到尾没被打开过。
2. **回环**：自己播出去的又读回来再播一遍，约一个块（1024 样本 ≈ 23ms）的
   重复回声，混音结果被自己的回声糊掉 → 识别率下降。

**立刻能做的**：控制台 → 设置 → 「电脑麦克风」下拉（`set-sys-mic`）把
「（系统默认）」改成 `麦克风阵列 (Realtek(R) Audio)`。

**该补的护栏**（待办）：程序不该允许把"电脑麦克风"选到自己正在播放的那根
虚拟线上 —— 选到就该拒绝并说清原因，而不是安静地转一圈回声。

---

## v1.0.12：修「波形在动、输入法却没声音」；并推翻 v1.0.11 对按键的结论

### 一、语音卡死（能开语音输入、有波形、却收不到声音，重启软件才好）

2026-09-17 真机现象：**语音输入功能打开着，界面上波形在动，但输入法收不到任何声音，
持续几分钟；重启软件立刻恢复。**

难点在于两个"看起来正常"的东西把根因盖住了：

- **波形在动** —— 它走的是 `on_audio → state.push_audio`，和**输出流无关**。
  输入流好好的，输出流早死了，界面上看不出来。
- **日志干净** —— 丢帧那句原来是 `logger.debug("Queue full, dropping frame")`，
  正式版日志级别是 INFO，等于**一声不响地把音频扔掉**（"静默丢弃"家族又一条）。

真正的两个根因都属于**"只有生产者、没有监护者"**：

| # | 问题 | 后果 |
|---|---|---|
| 1 | 输出流**建一次、start 一次**，之后没有任何监护 | PortAudio/WASAPI 那一路出一次事（设备被抢、睡眠唤醒、驱动抖动）就再也不回来 → `CABLE Input` 永远静音 |
| 2 | 混音回调**只在 `r_gain > 0` 时**才取队列 | 遥控器那一路静音/未参与混音期间队列一点都不消费 → 涨到 `maxsize=65536` 后每帧 `put_nowait` 都抛 `queue.Full` 被丢掉；重新出声时先播的是几分钟前积压的旧音频 |

修法（`main.py`）：

- 输出回调最前面写 `_cb_last_at` 心跳（**放在混合逻辑之前** —— 否则混合逻辑一抛异常
  就会被误判成"流死了"而反复重建）。
- 新增 `_supervise_audio()`，挂进主循环每轮检查：回调静默超过 **3 秒**（正常间隔约 15ms，
  即 200 个块）就判定这一路已停摆 → `_rebuild_audio()` 停掉旧流、重建、重启。
  两次重建之间至少隔 **20 秒**，避免死循环重建。
- 重建前 `_drain_audio_queues()` **清掉积压**：绝不能把"过去的声音"播出去。
- 混音回调改成**无论增益是否为 0 都按一比一消费队列** → 队列永远不会积压，
  `queue.Full` 这一类丢帧被从根上消掉。
- 队列真的满时累加 `_drop_frames` 并按 **第一次 + 每 200 次**报到 WARNING。

闸：`tools/check_audio_watchdog.py`（纯静态，含"不许退回老写法"的反例断言）。

### 二、推翻 v1.0.11 对按键的结论（重要，别再照旧路走一遍）

v1.0.11 的判断是「所有按键都发在**厂商自定义页**上，Windows 不处理厂商页，
所以自己去读那两路就能解出按键」。**真机实测把这个前提否掉了。**

v1.0.11 装在真机上之后：

- 程序确实**成功打开了 2 路厂商页**（日志：`✅ 遥控器厂商页按键已接管（2 路集合）`），
  但按遍所有键，**厂商页 0 条报告**、消费类控制 0 条、键盘钩子 0 条
  （同一份诊断里物理键盘的 Enter/Esc 各 3 次 —— 钩子是好的）。
- 于是下到 GATT 层直查（`tools/probe_gatt_hid.py`）：
  遥控器的 **HID 服务 `0x1812` 被独占**，`get_characteristics_async` 一律
  `status=3 (ACCESS_DENIED)`，按 UUID 单独取、`GattDeviceService.from_id_async`
  重开都被拒（`tools/probe_hid_claim.py`）。占它的不是本程序 ——
  本程序只开 ATVV，而 **ATVV 服务自己也是 `ACCESS_DENIED`**（被桥程序占着），
  两个服务各自被一个实体独占，指向的只能是 **Windows 的 HOGP 栈**。
- 查 devnode 绑定（`tools/probe_devnode_binding.py`）：HID 服务的驱动是
  **`mshidumdf`**（Windows 的 HID-over-GATT 驱动），`Problem=None`、`ConfigFlags=0`；
  5 路 HID 子集合的 Driver 都指向 HIDClass 类 GUID `{745a17a0-74d3-11d0-b6fe-00a0c90f57da}`。
  **也就是说 Windows 侧是好好绑着的** —— 之前把 `Service=(未绑定)` 当成"半坏"
  是误判，那对 HIDClass 子节点是**正常状态**。

结论：**Windows 的 HOGP 栈在正常工作，遥控器却没有往上送任何 HID 输入报告。**
"报错在没人读"（v1.0.11）不是根因，"根本没有报告流"才是。

顺带发现遥控器还有**两个私有服务**从未被检查过，且**没有被独占、我们订得上**：

| 服务 | 特征 | 可订阅 |
|---|---|---|
| `0000AE40-…` | `AE41` [write-noresp] / `AE42` [notify] | ✅ |
| `D343BFC0-…`（后缀与 ATVV 同厂） | `C1..C4` [write] / `C5` [notify] | ✅ |

另外每个 devnode 都有 **2~3 代实例**（`BTHLEDevice` 下 `9&1dc2feeb&0&` / `9&1dc2feeb&1&` /
`9&3703180e&0&`），与蓝牙里**两条 "Chromecast Remote" 配对记录**（当前
`04:7f:0e:f2:d2:94` + 旧 `04:7f:0e:90:11:01`）对应 —— 这是"注册表账本有好几代"的形态。

**新增排查工具（全部只读，不用退出桥程序，除最后一个）：**

| 工具 | 回答什么 |
|---|---|
| `tools/probe_gatt_hid.py` | GATT 层直探：全部服务/特征/属性，HID 服务能不能拿到 |
| `tools/probe_hid_claim.py` | HID 服务被拒之后，换三个入口能不能"要"过来 |
| `tools/dump_report_descriptor.py` | 各集合的 HID 报告描述符（并记下 `0x000B0193` 返回的是 `HidP KDR` 结构、**不是**原始描述符） |
| `tools/probe_devnode_binding.py` | 遥控器每个 devnode 的 `Service`/`ConfigFlags`/`Driver` |
| `tools/watch_all_channels.py` | **一条命令把 5 条通道同时挂上**（键盘钩子 / 5 路 HID 集合 / `AE42` / `D343BFC5` / ATVV CTL 每一个字节）→ 按一次键就知道它走哪条路 |

### 三、桌面启动脚本点不开

桌面那个「启动语音桥（源码）.bat」原来是 **UTF-8 无 BOM + LF 换行**，
第 2 行才 `chcp 65001` —— 而 CMD 在中文 Windows 上是**边读边按 GBK 解析**的，
读第一行时还没执行到 chcp，于是把中文注释的字节当成命令名，
直接报 `'勪簡銆?REM' 不是内部或外部命令`，脚本当场崩。

改成 **GBK 编码 + CRLF 换行 + 无 BOM**，`chcp` 那行去掉。文件名也简化成
`启动语音桥(源码).bat`（旧的全角括号那份已删，免得点错）。

> 教训：中文 Windows 上的 `.bat` **不要用 UTF-8 写**，哪怕加了 `chcp 65001`
> 也救不了 —— CMD 是先按当前代码页解析、后执行 `chcp`。

---

## v1.0.11：**按键映射终于能用** —— 自己读遥控器的厂商自定义页

### 根因：按键压根没进 Windows

「除了语音键，其它按键一个都没反应」这个问题从 v1.0.4 一直挂着，
v1.0.7 只是把**盲区照亮**（认不出的键不再静默丢弃），没解决根因。

遥控器（VID `18D1` / PID `9450`）暴露 **5 路 HID 集合**：

| 集合 | 用法页 | Windows 会做什么 |
|---|---|---|
| 键盘 | `0x0001/0x0006` | ✅ 变成键盘事件 |
| 消费类控制 | `0x000C/0x0001` | ✅ 变成媒体键 |
| 鼠标 | `0x0001/0x0002` | ✅ 变成鼠标事件 |
| **厂商自定义** | **`0xFF01/0x0001`（21 字节）** | ❌ **什么都不做** |
| **厂商自定义** | **`0xFF80/0x0000`（21 字节）** | ❌ **什么都不做** |

它把**所有按键**都发在那两个厂商页上 —— **Windows 完全不处理厂商页**，
既不产生键盘事件也不产生媒体键事件。于是：

- 键盘钩子**永远**收不到（日志里连一行 `🔘` 都没有，那不是"没映射"）
- 映射表改到天亮也没用 —— **事件根本没进 Windows**

验证过的两条证据：

1. `remote-diag.txt`（2026-09-17 01:31）里，键盘钩子 0 条、**5 路集合全 0 条**，
   而同一份报告里物理键盘的 Enter / Esc 各被记到 3 / 4 次 —— 钩子是好的，
   是遥控器没有产出任何 Windows 能看见的事件。
2. 参考实现 `VincentKingHsu/vRemoter`（macOS，同一支遥控器）的
   `ChromecastRemoteHIDBridge.swift` **也是自己打开厂商页读原始报告** ——
   它的打开流程没有任何握手/初始化，**开了就能读**，我们照做即可。

### 做法

新增 `remote_hid.py`：

- `CreateFileW` 打开遥控器的 HID 集合（复用 `hidwatch.HidCollection`，
  它已经处理好标志位和读线程），**厂商页解出按键并派发**；
- 非厂商页（键盘/消费类/鼠标）也打开，但**只记日志不派发** ——
  Windows 自己会处理它们，我们再发一次就是双份；记下来是为了留"落点"证据。
- 报告格式（与参考实现一致，本机 21 字节报告也对得上）：
  `reportID == 0x01`，第 2 字节就是用法码，`0` = 松手。
- 用法码与 `config.CHROMECAST_BUTTONS` 的 `usage` **逐条对上**
  （`0x03`=上、`0x07`=确认、`0x0B`=返回、`0x0A`=Home、`0x0C/0x0D`=音量…），
  已与 vRemoter 的 `chromecastButtons` 表交叉核对过。
- 语音键不在这条路上（它的 usage 是字符串 `"voice"`，走 ATVV），两条路不打架。
- 遥控器重连后 HID 设备路径可能整条换掉、读线程也会自己退出 ——
  **每 10 秒重扫一次**重新挂载，不会出现"重连后按键又不灵"。
- 长时间运行会裁剪已消费的报告缓冲，不会把原始报告一直堆在内存里。

解出来的 `button_id` 直接喂给原有的 `resolve_button`，
**映射表、控制台、`config.json` 全都不用改**。

### ⚠ 顺带改了默认映射（这是本版最容易漏掉的一条）

`DEFAULT_KEYMAP` 里方向键原来是 `"native"`（原样直通）。
那个默认值的前提是"遥控器的方向键 Windows 本来就收得到" —— **这个前提是错的**，
留着它等于上下左右永远没反应。现在改成真正注入方向键，与 vRemoter 的默认值一致。

同时把出厂留空的 4 个键（YouTube / Netflix / 电源 / 信源）也给了默认动作
——「装上就要能用」，不该让用户先去界面上填一遍才发现能按。

`CONFIG_VERSION` 升到 **2** 并补迁移：只搬**还停在旧默认值**上的那几项，
用户自己改过的一律不动。

> 已知代价：当初**故意**把方向键设成"原样直通"、或故意禁用某个键的用户，
> 会被覆盖回默认值。区分不了"没动过"和"故意设成一样的值"，
> 但比留着一堆没反应的键强，改回来只要点一下。

### 新增排障工具

`tools/check_remote_hid.py`：

- 不带参数 —— **不用按遥控器**就能验解码表和报告格式（纯单测）
- `--list` —— 列出当前 HID 集合，标出哪些是遥控器、哪些是本模块接管的厂商页
- `--watch N` —— 开 N 秒实时监听，按遥控器上的键直接看解出来的按钮名

### 排障口径（写在这里是因为"按键没反应"有四种修法完全不同的成因）

| 日志里有什么 | 说明 | 下一步 |
|---|---|---|
| `🔘 厂商页按键 → 按钮「x」按下` | ✅ 键到了程序 | 没动作 → 去控制台查这个键的映射 |
| `📥 集合 … 收到原始报告 …（非厂商页）` | 键落在了别的页 | 把这段发出来，映射要换路子 |
| `⚠ 没找到遥控器的厂商页 HID 集合` | 集合没打开 | 遥控器没连上 / VID 不对，跑诊断 |
| **以上都没有** | 报告根本没来 | 问题在蓝牙连接那层，不是映射层 |

---

## v1.0.10：**一键修好**「换 USB 口 → 本地地址变 → 配对记录作废」

v1.0.9 把这类故障**诊断**出来了（不再静默崩溃、不再骗人说「按遥控器任意键唤醒」），
但处置还是要用户自己「删设备 → 遥控器进配对模式 → 重新添加」——
而验证的时候发现**这条处置根本走不通**：设置里删这个设备会**卡死在「正在删除设备」**。

v1.0.10 把它变成可以自动修好的事。

### 一、三段真相（都是真机注册表里挖出来的）

**① 配对记录是"按本地无线电地址"存的**

```
HKLM\SYSTEM\CurrentControlSet\Services\BTHPORT\Parameters\Devices
      \f196a263671c\ServicesFor047f0e901101      ← 绑的是旧地址
```

**② 这颗蓝牙棒的本地地址是"按 USB 实例"缓存的**

| USB 实例 | 位置 | DeviceAddressCache |
|---|---|---|
| `6&2ac08ccf&0&2` | Port_#0002.Hub_#0002（**在用**） | `047f0ef2d294` |
| `6&2ac08ccf&0&3` | Port_#0003.Hub_#0003（旧口，PnP 里已不存在） | `047f0e901101` |

同一颗棒子、同一个 OUI（`04:7F:0E`），**只有后 3 字节不同** ——
证明地址不是棒子里烧死的，而是**按插在哪个口**变的。
换口 → 地址变 → ① 那张记录就作废了。

**③ 设备枚举出来的 ID 里带的还是旧地址，所以栈造不出设备对象**

`Enum\BTHLE\Dev_f196a263671c\<实例>\Device Parameters\Bluetooth_UniqueID`
= `Dev_047f0e901101_f196a263671c`（两个实例都是旧地址）。

于是：AQS 枚举得到 → `from_id_async()` 抛 `E_INVALIDARG` → `unpair_async()` 返回
`Failed` → 设置里「删除设备」**卡死**（删除也是照着同一张废记录去做的）。
**删除这条路是死的**，所以"删了重配"在真机上根本执行不下去。

**④ 「删设备」为什么卡死 —— `Properties` 的 ACL 只放 SYSTEM**

```
Enum\BTHLE\Dev_f196a263671c\8&1b58e5cc&0&f196a263671c\Properties
```

提权实测（2026-09-15）：这个键**连 `READ_CONTROL` 都 `rc=5`**，
管理员身份的 `RegOpenKeyExW(WRITE_OWNER)` 也一样被拒 —— 因为它有**独立且拒绝继承**
的 DACL。所以设置里「删除设备」永远卡死，`reg delete` 也删不掉。

**这不是"权限不够"，是"ACL 把你锁在门外"。** Windows 给的唯一正规出路是
**先成为所有者**（`SeTakeOwnershipPrivilege` + `SeRestorePrivilege` + `SeBackupPrivilege`，
管理员本来就持有这三个、默认是 DISABLED），改完 DACL 再删。

> 顺带记一个坑：只开 `SeTakeOwnershipPrivilege` **不够** —— 实测 `WRITE_OWNER`
> 照样 `rc=5`。绕开 DACL 的正主是 **`SeRestorePrivilege`**；少开一个，
> 症状和"权限不够"长得一模一样，能白查半天。

### 二、新增：`pairing.py` + 「修复蓝牙配对」入口

`python pairing.py` = **只读诊断**（不需要管理员）；
`--fix-pairing`（安装版：开始菜单 →「修复蓝牙配对」）= 自动修。

修复按阶梯来，每步都**先备份再动**：

| 方案 | 做什么 | 代价 |
|---|---|---|
| `migrate`（默认第 1 步） | 把配对记录搬到**当前地址**下（复制 `ServicesFor` + 搬链接密钥 + 同步节点的 `Bluetooth_UniqueID`） | 不碰地址，**不用重新配对** |
| `rebuild`（默认第 2 步） | 把**关联节点**（`Enum\BTHLE\Dev_<远端>`）删掉，让 Windows 按**当前**适配器重建 | 记录留着，**不用重新配对** |
| `restore`（降级，仅 `--method`） | 把**当前这颗棒的地址**改回记录绑的那个 | ⚠️ 实测**改不动**：写进 `DeviceAddressCache`、重启设备与服务之后读回来还是原值（驱动自己会写回） |
| `purge`（兜底） | 清掉废记录与僵尸节点 | 需要**重新配对一次** |

**为什么默认不碰地址、而是"搬记录 + 重建节点"**：设备枚举出来的 ID 是从节点的
`HardwareID` / 设备接口来的，**不只是** `Bluetooth_UniqueID` 那个字符串 ——
所以"只改 ID"实测无效（改完枚举出来的 ID 里还是旧地址）。唯一能让它变对的办法是**重建**。

- 动注册表之前用 `reg export` 落一份 `.reg` 到 `%APPDATA%\remote-voice-bridge\backup\`
  （`--restore-backup` 一键导回）
- 删节点之前**先探能不能删干净**：宁可不删，也不要"半删"
  （半删的症状是 `Device Parameters` 掉了、主键还在、调用方还当成功 —— 真机上发生过）
- 修完自动重启蓝牙栈，并**在新进程里**复读地址验收（同进程内 WinRT 会把地址缓存住）
- 最后真的把 BLE 设备打开一次 —— 这是唯一可信的验收标准
- 删节点之后要**等遥控器醒来**（按任意键）Windows 才重建；判据是"重建出来一个
  **不是幽灵**的节点"，而不是"有节点存在"（只看存在会打印假成功）

顺带解决一件事：**先看密钥材料还在不在**（`Parameters\Keys`，该键只放 SYSTEM、
非管理员一律 Access Denied）。密钥没了就直接 purge —— 免得白折腾一轮。

**`--take-ownership`：ACL 把门锁上时唯一的路**

```
python pairing.py --fix --method rebuild --take-ownership        # 保留记录，不用重配
python pairing.py --fix --method purge  --yes --take-ownership   # 兜底，要重配
```

它**一层层往下吃**：第 1 轮只解开 `Properties`，第 2 轮才轮到它底下的 `{GUID}` 子键
（那些子键各有独立且拒绝继承的 DACL，父键的继承 ACE 传不下去），一直到整棵树可删为止
——真机实测 3 轮、10 个键。**拿不到"现在真能删"的确认就绝不动手**，所以它要么干净删掉、
要么什么都不做（不拿半删去赌）。

⚠️ 它会**改变系统键的 ACL**（所有者 SYSTEM → Administrators），所以**默认不开**，
也不会在提权时被悄悄带上。

### 三、程序侧：不再拿一张废记录反复撞墙

`find_remote` 现在会把「设备 ID 里的本地地址 ≠ 当前无线电地址」的候选**直接排除**，
并一次性说清「记录绑的是谁 / 现在是谁 / 该点哪」。

以前它每轮重连都要拿那张废记录去 `from_id_async` 撞一次，
日志刷屏却一句有用的话都没有（这正是 v1.0.9 花两小时的原因之一）。

### 四、新增自检：配对判定链

`tools/check_pairing.py`：把判定逻辑抽成纯函数 `classify()`，用假数据把真机形态
（记录绑旧地址 + AEP 节点也是旧地址）钉成 `STALE_ADDR`，覆盖 6 种状态
（`OK` / `STALE_ADDR` / `NODE_PHANTOM` / `RECORD_OK_NO_NODE` / `RECORD_NO_ADDRESS` / `NO_RECORD`），
再拿**行为级反例**（把 `classify` 抠出来改坏、exec 起来必须复现错误判定）
和**源码级断言**锁住每一处踩过的坑：

- **PnP 实例 ID 必须带 `USB\` 枚举器前缀** —— 少了它 `CM_Locate_DevNodeW`
  对所有设备都返回 `CR_NO_SUCH_DEVINST(13)`，于是**正在用的那颗棒子被标成历史残留**。
  这个错法不报错、只给反结论，比崩溃更难发现（第一版就踩了）。
- **`STALE_ADDR` 必须排在 `NODE_PHANTOM` 前面** —— 真机形态下节点同时也是过期幽灵，
  顺序反了会把"记录作废"这个根因盖掉，工具就去"重建节点"而不是"搬记录"了。
- **「幽灵」的口径不能只看活地址** —— 只数活地址上的幽灵，`migrate` 之后那种形态会掉进
  `UNKNOWN`（报告里显示「看不出来」），把清楚结论说糊（真机上就是这么显示的）。
- **搬家时保留注册表值类型** —— `ServicesFor` 底下混了 REG_DWORD 和 REG_QWORD，
  照 DWORD 写回去会静默截断。
- **「删不动」这个结论只在管理员下给** —— 非管理员跑探针，普通用户连
  `Enum\BTHLE` 整棵树都写不动，探出来必然"全都删不动"，而报告会把它归因成
  「ACL 只放 SYSTEM，管理员也删不掉」→ 等于把用户直接劝退到"没救"。**结论错比不给结论更糟。**
- **探可删性要用 `DELETE` 权限** —— 删键要的是它，用 `KEY_SET_VALUE` 会漏判
  "能写、不能删"那种 ACL。
- **ACL 接管要开一组特权 + 循环到不动点** —— 少开 `SeRestorePrivilege` 会被拒；
  一轮只解一层。
- **提权时把所有开关原样带给子进程** —— 漏带一个，子进程就静悄悄少做一步，
  而症状和"那一步不生效"长得一模一样（真机上就这么丢过 `--take-ownership`）。

`tools/check_packaging.py` 也加了 3 条：`pairing` 必须进诊断 EXE 的 `hiddenimports`、
开始菜单必须有 `--fix-pairing` 入口、`.bat` 必须被 `[Files]` 拷进安装目录。
（**"CI 绿"不等于"包里真的有"** —— v1.0.5 的教训。）

### 五、怎么用

| 情况 | 做什么 |
|---|---|
| 程序显示「未连接」，Windows 显示「已配对」 | 开始菜单 →「修复蓝牙配对」→ UAC 点「是」 |
| 修完等它重建 | **按一下遥控器任意键**（最多等 60 秒）；超时也没事，遥控器下次一醒会自动重建 |
| 报「关联节点删不动」 | 加 `--take-ownership`（见上） |
| 想知道"谁挡着不让删" | `python pairing.py --acl-dump`（提权，只读，列出所有者 + 每条 ACE） |
| 想知道"等重建就行"还是"必须重配" | `python pairing.py --keys`（提权，只读，列链路密钥树） |
| 只想看看、不动任何东西 | `python pairing.py`（只读）；`--acl-probe` 只探"节点删不删得动" |
| 只想验"方案选得对不对" | `python pairing.py --fix --dry-run`（不提权、什么都不改） |
| 修完还是不行 | 报告在 `%APPDATA%\remote-voice-bridge\pairing-fix.txt`，发出来 |

**根治建议**：这颗蓝牙棒**固定插一个口别换**；机器上还有别的蓝牙（本机的英特尔板载
就是个幽灵）就在设备管理器里禁掉，免得它抢。

> 本文里的所有结论都来自真机注册表实测（`Enum\USB` / `Enum\BTHLE` /
> `BTHPORT\Parameters\Devices`），不是推测。反例与断言见 `tools/check_pairing.py`。

---

## v1.0.9：修「程序显示未连接 / Windows 显示已配对」—— 报错被丢进虚空 + 配对记录作废

真机反馈（2026-09-15 上下午各一次）：托盘一直显示
「○ 未连接（按遥控器任意键唤醒）」，可 Windows 设置里「Chromecast Remote」
明明写着**已配对、电量 88%**。

### 一、根因一：桥每 3 秒崩一次，而**报错被扔掉了**

`bridge.log` 里 4 分钟刷了 71 轮启动横幅，每轮都在 `🔗 Connecting...` 之后
戛然而止，**一行报错都没有**；Windows 事件日志也查不到（Python 层异常不是进程崩溃）。

原因是一行代码 + 一个打包选项叠在一起：

| 组件 | 干了什么 | 后果 |
|---|---|---|
| `main.py` | `from_id_async()` 抛 `OSError: [WinError -2147024809] 提供的设备 ID 不是有效的 BluetoothLEDevice 对象` | 异常逃出 `run_bridge` |
| `tray_app._bridge_worker` | 用 `print()` + `traceback.print_exc()` 报 `[bridge] …` | 主 exe 是 `console=False`（不分配控制台窗口）→ **输出流向是空的** |

`print` 在无控制台的 exe 里等于把唯一的线索直接扔掉。
**现在异常走 `logging`（→ `bridge.log`），带完整 traceback。**

### 二、根因二：配对记录绑在**另一块蓝牙无线电**上

真正连不上的原因（注册表 `BTHPORT\Parameters\Devices\<远端MAC>\ServicesFor<本地适配器MAC>`）：

| 项 | 值 |
|---|---|
| 配对记录绑在的本地地址 | `04:7F:0E:90:11:01` |
| 当前生效的无线电地址 | `04:7F:0E:F2:D2:94`（BARROT USB 蓝牙） |

设备能被枚举到、Windows 设置里也显示「已配对」，但蓝牙栈造不出可用的设备对象 ——
`from_id_async` 抛 `E_INVALIDARG`，`from_bluetooth_address_async` 返回 `None`。
**怎么重试都一样**，这不是软件能自己恢复的。

**处置**：设置 → 蓝牙和其他设备 → 删掉「Chromecast Remote」→ 让遥控器进入配对模式
（长按 Home+返回 约 3 秒）→ 重新添加。

> 换 USB 口 / 换蓝牙模块会让这块 dongle 的**本地地址变化**，旧配对记录随之作废。
> 「上午在 T420 上装也遇到过」就是同一个问题。

### 三、改了什么

| 位置 | 改动 |
|---|---|
| `tray_app._bridge_worker` | 异常改走 `logging`（`exc_info=True`），不再 `print` |
| `tray_app._status_text` | 有具体原因就报具体原因，不再无条件说「按遥控器任意键唤醒」 |
| `main._open_ble_device` | 新增：主路径按 ID → 备用路径按远端 MAC → 都失败就把「地址对不上 + 处置」写进日志，并写进 `state` 供托盘/控制台显示 |
| 重试节奏 | 连续失败退避 3s → 10s → 30s（原来固定 3s，故障时 4 分钟就把日志撑到 47 KB，线索被淹没） |
| `tools/check_failure_visibility.py` | 新增回归闸（17 项，含 3 个反例） |

### 四、新增自检：故障可见性

`tools/check_failure_visibility.py` 用**静态 + 反例**锁住四条：桥线程异常必须走
logging、连接失败必须「为什么 + 怎么办」且不只试一条路、托盘必须能说出具体原因、
`_open_ble_device` 必须真的被 `run_bridge` 调用（写了没接上等于没写）。

反例的逻辑是"把源码改坏，闸必须翻红" —— **一个永远绿的闸比没有闸更危险**。

---

## v1.0.8：修控制台「遥控器麦克风」永远显示「等待语音」+ 补上「按键没反应」的盲区

真机反馈（2026-09-15）：语音键按一下已经能说话了，但控制台「音频」页里
**「遥控器麦克风」一直显示「等待语音」、电平条一格不亮**，而它的**波形却在动**
—— 用户原话是"遥控麦克风有波形，但是它显示在整个没反应"。
另外「静音键按了没反应」。

### 一、根因：这一格读数**没有生产者**

「遥控器麦克风」那张卡上的状态与电平条，读的是 `state.remote_level_db`，
而它只由 `state.push_levels(remote_db=…)` 写入 —— 但**整个项目里没有任何一处
产品代码传过 `remote_db`**（只有 `tools/serve_console.py` 那个假数据演示服务器传过）。

后果：真机上它永远是初始值 **−96 dBFS**。

| 界面元素 | 数据来源 | 真机表现 |
|---|---|---|
| 波形曲线 | `state.push_audio()` → `_wave` | ✅ 正常（所以"有波形"） |
| 状态文字 / 圆点 | `remote_level_db` | ❌ 永远「等待语音」 |
| 电平条（14 格） | `remote_level_db` | ❌ 永远一格不亮 |

`active(db)` 的判据是 `db > DB_FLOOR(-60)`，−96 恒不成立，所以那两格永远不动。
**根因不是音频，是"只有消费者、没有生产者"** —— 这也解释了为什么音频明明在流、
说话却完全正常。

修法：在 ATVV 解码回调（`on_audio`）里按**原始解码峰值**算 dBFS 并写入
`push_levels(remote_db=…)`。用原始峰值而不是那个 0-100 的 `level`，
是为了和另外两路同一把尺子（UI 显示的就是 dB）。

### 二、顺带修掉一个会把它再抹掉的坑

`build_state()` 先展开 `build_live()`，然后又**覆盖了一遍 `levels`**。
也就是说 `build_live()` 里刚做的任何电平处理，都会被那句覆盖当场作废 ——
同一份数据两个来源，改了一个另一个悄悄赢。现在电平/波形只有 `build_live()` 这一个出处。

### 三、电平过期保护

遥控器那一路的电平是**按帧喂**的，一旦停止推流就没人把它刷回去，读数会冻在
最后一帧上 —— 界面就一直显示"有声音"、电平条一直亮着。
现在超过 **0.7 秒**没有新音频帧就判为没声音（正常约 20ms 一帧，余量两个数量级）。
另外两路不需要：电脑麦克风与混音输出都有常驻回调在持续写。

### 四、「某个键没反应」的盲区

这是「静音键按了没反应」查不下去的真正原因 —— 两条路径都是**静默丢弃**：

1. 认不出的**蓝牙控制指令**：`on_control` 只在 **DEBUG** 打一行然后 `return`，
   而正式版日志级别是 INFO → 日志一片空白
2. 认不出的**按键名**：`_on_key` 里映射表查不到就什么都不做也不留痕

于是既无法证明"遥控器发了这个键"，也无法证明"没发"，只能靠猜。
现在**每个键名首次出现时都报一次**（之后静默，物理键盘最多吵几十行），
而且带上"它被认成哪个按钮、映射成什么动作"——一次按键就能定性：

```
🔘 HID 按键 'volume mute'（scan=173） → 按钮「mute」→ 动作 'voice_ptt'
🔘 HID 按键 'enter'（scan=40） → 按钮「ok」→ 动作 'enter'
🔘 HID 按键 'f13'（scan=100） → 没有对应按钮，已忽略（若是遥控器上的键，请到控制台「按键映射」页给它指定动作）
🔘 收到未处理的控制指令 0x0B（len=2） 原始=0b 00
```

排查口径从此固定下来：

| 日志里 | 含义 | 往哪查 |
|---|---|---|
| 有 `🔘 HID 按键 …` | 键已经进了 Windows | 我们这层或下游（映射值/注入） |
| 一个字都没有 | 键根本没进 Windows | 蓝牙 / HID 那一层，不是本程序 |

> 顺带说明：本次日志里 **34 条 `voice hotkey DOWN` 全部紧跟「语音会话【开始】」**，
> 即 100% 来自蓝牙语音键；**静音键在整份 1672 行日志里一次都没留下痕迹**。
> 它走的是键盘通道（HID），而那条路径此前不写任何日志。
>
> Windows 侧已核实（2026-09-15）：遥控器 `Chromecast Remote` **确实被挂成了 HID 键盘**
> （`Service=kbdhid`、`keyboard.inf`、`ConfigFlags=0`，另有 consumer control / mouse /
> vendor-defined 四个集合）。也就是说按键**本来应该能变成 Windows 按键** ——
> 「除了语音键其他键全没反应」到底卡在哪一段，现在靠上面这张表一次按键就能定位。

### 五、「静音键」与「语音键」的日志不再撞车

两条路径最后都落到 `hotkey_down()`，打出来是同一行
`🎤 voice hotkey DOWN (hold): win+ctrl` —— 只能靠"有没有紧跟一条会话开始"去反推。
现在 PTT 那条会带上按键名：

```
🎤 按键「mute」→ 按住说话 PTT（ctrl+win）：按下
```

### 六、新增回归闸：`tools/check_levels.py`

一类专门防"界面有这一格、但没人喂它"的检查：

- ① 三路电平各自都有生产者（在真实产品代码里，不是演示脚本里）
- ② 控制台真的在读这三个字段（改名漏改一处就报警）
- ③ 动态验证：`push_levels(remote_db=)` 真的写进去了、`push_audio()` 真的往波形里写点、
      `clear_audio()` 会把遥控器电平归零

**带反例自检**：篡改源码把生产者删掉后必须报 FAIL —— 否则这颗闸可能只是个
永远绿的摆设，那比没有更糟。已接入 `tools/check_all.py`（本地与 CI 都跑）。

### 七、按键映射里补上「换行」

反馈原话：「映射按钮那里竟然没有选项，竟然没有"换行"，因为输入里面经常要换行」。
表里原先只有 `Enter` 一项，名字还叫"回车 / 确认" —— 按"换行"去找根本找不到，
只能拿 Enter 硬试：试对了是运气，试错了就把没写完的消息发出去。现在：

| 目标 | 什么时候用 |
|---|---|
| `回车 / 换行 / 确认 Enter` | 多行输入框（评论区、富文本、代码框）里 Enter 本身就是换行 |
| `换行（Shift+Enter）` | 微信 / QQ 这类「Enter 发送」的聊天框 |
| `换行（Ctrl+Enter）` | 部分输入框用 Ctrl+Enter 换行 |

> ⚠ 一个已知取舍：语音会话进行中按**确认键**会被程序吃掉用来收尾
> （见 `swallow_ok_during_voice`）。想在说话的同时打换行，把「换行」映射到
> **别的键**上 —— 音量键、YouTube / Netflix 键都还空着。

### 八、「不让电脑麦克风参与，它还是在输出」

根因两处，都是"界面上的开关管不到后端"：

1. **没落盘**：音频页的「参与混合 / 增益」只改内存里的 `state._mix`，`config.json`
   一个字没动。于是只要在设置页改了任何一项、或者重连/重启，
   `_patch_config` / `run_bridge` 就会拿 `config.json` 把 `set_mix` **重灌一遍** ——
   用户的勾选被悄悄回滚。
2. **被白名单丢掉**：`/api/config` 的 `allowed` 里**没有** `system_mic_enabled` /
   `remote_mic_enabled`，前端发过来就被静默忽略，后端仍按旧配置把这一路混进去。

修法：

- `/api/mix` 新增 `_patch_mix()`，把在配置里有对应项的四项落盘
  （`sys_enabled→system_mic_enabled`、`remote_enabled→remote_mic_enabled`、
  `sys_gain→system_mic_gain`、`remote_gain→gain`）；静音/独奏是临时操作，故意不落盘。
- `/api/config` 白名单补齐，并把被忽略的字段**记日志 + 回给前端** ——
  静默忽略是不可调试的：前端以为改成功了，后端当没听见。
- 界面也说清楚：没参与混音的那一路，状态词直接写成「未参与混音」/「已静音」，
  整张卡压暗。麦克风一直在采集、电平条和波形照旧在动，不写明就会被当成"关不掉"。
  混合输出卡的小结同理：「两路都没送出去（被静音或没参与）」。

### 九、状态清单里不再替 HID 打包票

「遥控器连接」原来写"BLE · HID 已就绪"。可蓝牙连上 ≠ 遥控器按键就能变成
Windows 按键（2026-09-15 的真机上正好是语音键好使、其他键全没反应），
这句话会把人往错方向带。改成
「蓝牙已连接（若按键无反应，请跑一次「遥控器诊断」）」，把排查入口直接指出来。

### 十、把遥控器的 HID 真身挖出来了：**两个厂商自定义页，Windows 根本不看**

反馈原话：「你那个遥控器上面的按钮，除了语音输入有用，其他所有按键都没有作用」。

这条反馈里藏着一条**极其干净的分界线**，以前没人利用：

| 走哪条通道 | 真机表现 |
|---|---|
| 语音键 → ATVV（BLE GATT 自定服务） | ✅ 能用 |
| 其余键 → HID（HOGP 蓝牙键盘协议） | ❌ 全废 |

也就是说 —— **BLE 链路、GATT、音频解码、ATVV 代码全是好的**，
问题只可能在 HID 这一层。于是去看 Windows 自己的账本（注册表 + HID 描述符），
真机上挖出这条硬事实：

遥控器 `VID 0x18D1 / PID 0x9450` 只暴露 **5 个 HID 集合**：

| 集合 | 用法页/用法 | 含义 | Windows 会做什么 | 输入报告 |
|---|---|---|---|---|
| Col01 | `0x01/0x06` | 键盘 | ✅ 变成键盘事件 | 9 字节 |
| Col02 | `0x0C/0x01` | 消费类控制 | ✅ 变成媒体键 / 浏览器键 | 4 字节 |
| Col03 | `0x01/0x02` | 鼠标 | ✅ 变成鼠标移动 / 点击 | 5 字节 |
| Col04 | **`0xFF01/0x01`** | **厂商自定义** | ❌ **什么都不做** | 21 字节 |
| Col05 | **`0xFF80/0x00`** | **厂商自定义** | ❌ **什么都不做** | 21 字节 |

两个厂商页的输入报告都是 **21 字节**（键盘那一路才 9 字节）。
Windows 的 HID 栈**不会**把厂商页的报告翻译成任何按键事件 ——
若遥控器把按键状态发在这两页上，那么"改映射表"**永远不可能有效**，
因为事件根本没进 Windows。

> 换句话说：这一条把「除语音键外全部按键没反应」拆成了四种**修法完全不同**的成因，
> 而它们在日志上长得一模一样：
>
> | 现象 | 成因 | 该修什么 |
> |---|---|---|
> | 报告根本没来 | 蓝牙没连 / 遥控器没发 | 蓝牙层 |
> | 报告落在厂商页 | Windows 不翻译 | **自己去读那两路、按字节解按键**（改映射没用） |
> | 报告落在鼠标页 | 键盘钩子看不见 | 按鼠标事件处理 |
> | 键盘钩子收到了 | 真的到 Windows 了 | 映射值 / 拦截开关 |
>
> 顺带说明：本机实测键盘那一路是**健康**的（`Service=kbdhid`、`ConfigFlags=0`），
> 所以"设备没被认成键盘"这条可能性在**本机已被排除**；剩下三种仍需一次按键来分。

### 十一、新增两个探测模块 + 一个监听工具（不用按任何键就能查一半）

| 新增 | 作用 |
|---|---|
| `hidinfo.py` | **不用按任何键**。读注册表 + `HidP_GetCaps`，报出遥控器的 5 个集合各自"是什么、Windows 会不会处理"、键盘那一路有没有被绑上（`Service=kbdhid`）、是否被禁用（`ConfigFlags`）、蓝牙地址；顺带把**生效中的 config.json** 关键项列出来（映射总开关 / 拦截开关 / 空映射的键）—— "什么都没发生"的另一半原因常常就在这里 |
| `hidwatch.py` | 同时开三路监听：键盘钩子 + 各集合的**原始输入报告流**（阻塞 `ReadFile`，一路一个守护线程）。一次按键就能看出它落在哪一路 |
| `tools/watch_reports.py` | 上面那个的命令行版（源码环境用，默认听 40 秒） |

诊断工具也跟着升级：

- **`python tools\diag_remote.py --hid`**（安装版：`RemoteVoiceBridgeDiag.exe --hid`）
  2 秒出结果，**不用按任何键，也不用先退出桥接程序**。
  「蓝牙显示连好了、但按键没反应」先跑这个 —— 一眼分清是"Windows 没认成键盘"
  还是"程序这层的事"。
- 常规 7 段诊断的报告里，**开头多出【结论 0】硬件身份**（不用按键的那部分），
  **末尾多出【结论 4】按键落点**（各集合原始报告计数 + 判读）。
  两段合起来才能把上面那张四行表填满。

> ctypes 踩坑记录（都写进 `hidinfo.py` 文件头了，别再犯）：
> · `SP_DEVICE_INTERFACE_DETAIL_DATA_W.cbSize` 在 64 位是 8、32 位是 6，
>   写错**不报错**、只给一条残缺路径，后续 `CreateFileW` 直接 `ERROR_INVALID_NAME(123)`。
>   现在不依赖偏移，直接在缓冲区里找 `\\?\` 前缀。
> · 取报告描述符的 IOCTL 是 **`0x000B0193`**（不是网上常见的 `0x000B0190`，
>   实测 0x190 返回 err=1）；而且它返回的是 hidparse 的 `HidP KDR` 结构，
>   不是原始描述符，别拿去当描述符解析。
> · 句柄用 0 权限打开时 `HidD_GetAttributes` / `HidP_GetCaps` 能用，
>   但 `DeviceIoControl` 会 `ERROR_INVALID_FUNCTION(1)` —— 取描述符必须用 `GENERIC_READ`。
> · 键盘 / 鼠标集合打不开（`err=5`）是**预期结果**：`kbdhid` / `mouhid` 独占持有它们，
>   而且我们也不需要读（Windows 自己会翻译，走钩子就能看到）。报告里已写明，免得看着像 bug。

### 十二、补上最后一个静默盲区：**键名本身为空**

第四节的日志表有个洞：它管的是"键名认得出、但映射表里没有"。
还有一种更靠前的情况 —— `keyboard` 库遇到它解析不出来的键，会把 `e.name`
报成 `None`/空串。这种事件在 `_on_key` 的**每一个分支里都是 falsy**，
会一路静默滑到最后：不映射、不记日志、不报错。用户看到的就是"这个按键没反应"，
而日志连"它报了什么名字"都拿不到。现在补一条带 `scan` / `vk` 的日志：

```
🔘 HID 按键【无名】—— keyboard 库没能解析出键名（scan=…、vk=…）→ 无法映射，已忽略。
```

### 十三、新增回归闸：`tools/check_hidinfo.py`

守第十节那条硬事实（判定改错，报告就会指向错误的修法，用户会照错方向白折腾）：

- 静态：厂商页下界 = `0xFF00`；集合角色表里键盘/消费类判定为"Windows 会处理"、
  `0xFF01` 判定为"不处理"；IOCTL 码是 `0x000B0193`；
  **设备路径与 `cbSize` 偏移无关**（真的拿一个路径放在偏移 6 的 UTF-16 缓冲区去取）
- 动态：假数据跑两条判读分支（有键盘事件 / 只有厂商页报告）
- **反例**：抬走厂商页下界、或把所有集合角色改成"会处理"之后，
  报告里对应的警告**必须消失** —— 证明那些字是判定印出来的，不是硬编码凑的

> 写反例时踩的坑：标记串必须**各自唯一**。第一版用"厂商自定义"当标记，
> 结果两条反例都"失败"—— 因为结论 0 里另有一句与判定无关的硬编码说明也含这四个字。
> 另外反例二一开始只改 `_COLLECTION_ROLES` 里的一条，而遥控器有**两个**厂商页，
> 第二个（`0xFF80`）不在表里、走的是下界兜底分支，照样把同样的说明印出来 ——
> 所以必须整个换掉 `collection_role`。

---

## v1.0.7：修掉「按一下语音键立刻被关 + 全程 0 帧」

真机反馈（2026-09-15 11:08 的 `bridge.log`）：语音键按一下，混音**还是**卡在
「等待录音」、转文字**还是**逐字卡、**有时一松手就不再调用输入法**，静音键按住说话没作用。

### 一、根因：我们自己补发的 MIC_OPEN 引来的回响，被当成了「用户第二次按下」

真机日志时序（毫秒级，一字未改）：

```
36.906  ▶ Audio START              用户按下语音键
36.906  📤 MIC_OPEN 补发            连接后遥控器还没收到过开麦命令
36.944  📤 MIC_OPEN [0c 00]         写入成功
36.995  🎤 voice hotkey DOWN        会话开始
36.995  ▶ Audio START               ← 遥控器回"我开始推流了"
37.046  🎤 voice hotkey UP          ← 却按"用户第二次按下"处理了
37.046  🎙️ 语音会话【结束】（第二次按下语音键）
38.215  ⏹ Audio STOP （本次共收到 0 个音频帧，峰值 0）
```

第二个 `audio_start` 是**我们要来的回响**，不是用户又按了一次。程序把它当成
第二次按下，于是连环出事：

1. 立刻 `voice_hotkey_up()` → 输入法的语音被关掉（用户感觉"刚开就被关"）
2. `voice_active` 变成 `False` → 等真松手（`audio_stop`）时**不再补开麦**
3. 遥控器保持停推流 → **全程 0 帧**

「混音卡在等待录音」「转文字逐字卡」都是第 3 条的直接结果 ——
**不是输入法的问题，是根本没收到音频。**

修法：补发 `MIC_OPEN` 成功后立刻记一次 `mic_reopen_at`，复用已有的
`MIC_REOPEN_GRACE = 1.5s` 防自激窗口，把这次回响挡掉（实测只隔 51ms）。

> 这套防御本来就有 —— `audio_stop` 那条路径写对了，
> **首次补发这条路径漏了赋值**。一个变量的缺失，三种现象。

### 二、两个开关打架不再沉默

`voice_mode` 与 `hotkey_mode` 说的是同一件事，但**只有 `hotkey_mode` 对真机生效**：
`voice_mode` 只喂给 `session` 的 `START_SEARCH` 分支，而真机遥控器**从不发
`START_SEARCH`**（见 `session.ensure_mic_open` 的说明）。两者不一致时，
用户看到的行为和配置面板上写的对不上。

真机事故正是这个组合：`hotkey_mode` 停在 `"hold"`，而键位配的是微信输入法
「启动语音输入」（左Ctrl+左Win+左Shift，**切换式**）—— 程序却按「按住不放」
的方式发它，一松手自然就断了。

现在启动时发现不一致会明确警告（**只提醒，不擅自改用户配置**）：

```
[CONFIG] ⚠ voice_mode 与 hotkey_mode 不一致：voice_mode='toggle'，hotkey_mode='hold'
[CONFIG]    实际生效的是 hotkey_mode（当前 = 按住说话）；voice_mode 对真机无效（遥控器从不发 START_SEARCH）
[CONFIG]    请到控制台「设置 → 语音 → 触发方式」核对一下
```

### 三、诊断工具拒绝出「脏报告」

2026-09-15 那份 `remote-diag.txt` 是**在桥接程序运行期间**生成的，结果：

- 程序的拦截钩子把遥控器原生键**全吞了** → 报告写「遥控器 OK / 返回 / 音量 / 方向
  各 0 次」，看着像遥控器坏了，其实是**一个都没测到**
- 程序自己注入的按键又被记了进去 → 「其它键」那 3 条其实是注入的，不是遥控器发的

整份结论不可用。现在 `diag_remote.py` 启动时先查 `RemoteVoiceBridge.exe` 进程，
在跑就直接停下并说明原因（坚持要带着它测、自认报告作废：加 `--force`）。

---

## v1.0.6：诊断工具真正打进安装包

v1.0.5 发出去之后才发现一个**我自己的洞**：那一版加了遥控器诊断工具，
但它只是仓库里的一个 `.py` + 一个 `.bat`，**没有打进 Setup.exe**。

安装版用户机器上通常没有 Python，`diag-remote.bat` 对他们等于不存在
—— 等于 v1.0.5 宣传的那个工具，主要用户根本拿不到。
根因是 v1.0.5 只在源码环境里验证过，打包路径没测，这是我漏了。

### 修法

- `remote-voice-bridge.spec` 增加**第二个入口**：
  `RemoteVoiceBridgeDiag.exe`（带控制台窗口），与主程序共用同一套依赖目录，
  不额外增加安装体积的重复部分
- 安装后开始菜单多一项 **「遥控器诊断（按键没反应时跑这个）」**
- 打包版跑完会**停在窗口等回车**再关。否则双击运行、报告打印完窗口立刻消失，
  用户什么都看不到。源码版不重复暂停（`diag-remote.bat` 末尾已经有 `pause`）
- 诊断脚本不再依赖"自己所在目录的上一级是仓库根"：打包后 `__file__` 指不到仓库，
  改成只在源码环境才动 `sys.path`，包内的 `config` / `_utf8` 直接 import

### 为什么不能只改文档

`dist/RemoteVoiceBridge/` 里没有这个 exe，`installer.iss` 里的
`Source: dist\RemoteVoiceBridge\*` 自然也装不出它。必须让 spec 产出它，
所以这是一次实打实的再发布，而不是文档修补。

### 自检

- `check_all` 新增 `check_packaging.py`（6 项）：spec 里有诊断入口、
  入口脚本文件存在、它是 console 版、`COLLECT` 真的收了它、
  `installer.iss` 里的名字与 spec 一致、开始菜单真的引用了它
- 做了**负向验证**：把 `installer.iss` 里的引用故意改坏，它确实会红
  （不是写完就算数）
- 本地完整跑过一次 `pyinstaller remote-voice-bridge.spec`，确认
  `dist/RemoteVoiceBridge/` 里两个 exe 都产出，并且**打包版的诊断 exe
  真的能启动**：打印 v1.0.6、加载 `keyboard` 库、进入采集循环。
  上一版就是把这段留给了 CI，而 CI 只验证"构建成功"、不验证"包里有没有"

---

## v1.0.5：把"要用户描述现象"改成"让程序自己测出来"

v1.0.4 修掉了主线故障，剩下一批小缺陷和两个"必须真机才知道"的疑问。
本版把它们一起收掉。

### 一、同类缺陷扫尾（v1.0.4 只修了一处，这里补齐）

| 位置 | 问题 | 修法 |
|---|---|---|
| `console_server._resolve_out_device` | 注释写着"子串命中优先取最短的"，**代码其实返回第一个命中的**。装了 VB-CABLE + CABLE 2 时下拉框显示的和实际用的不是一只声卡 | 改成真的三级匹配（全名 → 前缀 → 子串）+ 同级取最短名 |
| `mixer.find_input_device` | 子串匹配同样是"谁先枚举到用谁"。同一只声卡在 MME / DirectSound / WASAPI / WDM-KS 下各出现一次，MME 还会把名字截断到 31 字符，枚举顺序由驱动决定 | 同上，三级匹配 + 取最短名 |
| `config.Config.load()` | keymap 里非字符串的值被**静默丢弃**，那个按键永远没反应、日志一个字都没有 | 打印告警，列出被丢掉的条目 |
| `main._on_key` 里的 `last_ble_activity` | 注释写成"遥控器按键 = 它还活着"，但低级钩子**分不出来源设备**，敲物理键盘同样会刷新它 | 注释改成实话，并写明这是**刻意接受的保守偏差**（宁可偶尔漏判失联，也不要因为你敲键盘就误判掉线、把遥控器哑掉 4 秒） |
| `tools/check_injection.py` | 「卡键」判定没有排除"本轮开始前就已经按着的键"，上一轮残留会被算成这一次没释放，报出假 FAIL | 减掉本轮开始前就已按下的键；重试轮次 3 → 6 |
| `main._send_tx` | 投递失败时协程既没被接管也没 `close()`，解释器 GC 时甩一条 `coroutine was never awaited` RuntimeWarning，把真正的 `schedule failed` 日志淹没 | 失败分支先 `coro.close()` 再打错误日志 |

### 二、映射值写错不再静默失效

`send_key`遇到解析不出来的键名会**静默跳过** —— 用户看到"这按键没反应"，
日志里一个字都没有。手改过 `config.json`、或从旧版本带过来的配置很容易踩到。

现在开机（以及每次热重载）都会把用户实际在用的 keymap 校验一遍，
发现发不出去的键名就打 WARNING，并写清是哪个按键、哪个值、去哪里改：

```
⚠ 按键映射「home」→ 'win+ctr' 里有发不出去的键名 ['ctr']，这个键按下去不会有任何反应。
  请到控制台「按键映射」页改掉。
```

> `check_keymap.py` 只在 CI 里校验**默认表**，管不到用户实际在用的那一份。
> 这里补的是运行时的那半边。同一个坏值只吵一次，不会刷屏。

### 三、新增：遥控器真机诊断（`diag-remote.bat`）

**目的：把"你描述现象 → 我猜原因"换成"程序测出来 → 直接看结论"。**

双击仓库根目录的 `diag-remote.bat`，按 7 段提示按键（约 90 秒），
产出 `%APPDATA%\remote-voice-bridge\remote-diag.txt`：

1. 遥控器每个键在 Windows 上**实际发出什么**（名称 + 硬件扫描码）
   → 直接回答「有些按键没作用」到底是"没发事件"还是"映射写错了"
2. 遥控器 OK 键 与 物理键盘 Enter 的 `(名称, 扫描码)` **是否相同**
   → 这一条是决定性的：相同则底层**无法**区分来源设备，
     只能靠"吞掉"拦截、代价是物理键也被吞；不同则可以实现
     只拦遥控器、完全不动物理键盘的精确方案

它的「报告生成器」是纯函数式的，所以 `check_all` 会用假数据把
两条分支（能区分 / 不能区分）都验一遍 —— 否则那段代码第一次运行就是在用户机器上。

### 四、`check_all` 不再被机器负载骗

`check_injection.py` 是唯一会真的注入按键、读系统实时状态的一关，也是唯一受负载影响的。
实测同一份代码连跑两次，一次报 2 项 FAIL、一次全过。

现在 `check_all` 跑它之前先等 2 秒（让前一步的进程退干净），
失败且原因是「钩子没收到」时整步重跑一次：
**噪声被滤掉，真回归仍然每次都会红。** 这不是"把测试调松了"。

---

## v1.0.4：修掉「松手就断流 / 转文字逐字卡顿」——MIC_OPEN 其实从未发出

> 🔴 这是 v1.0.3 的真机事故修复版。**v1.0.3 建议直接跳过，装这个。**

### 症状（真机）

1. 按下语音键能说话，但**一松手**，混音输出立刻变成「等待录音」，再也收不到声音
2. 即便勉强在跑，**语音转文字一个字一个字往外冒** —— 说了近 40 秒，文字慢慢爬
3. 静音键的「按住说话」完全没反应

### 根因：Windows 蓝牙回调线程里没有事件循环

Windows 的 BLE 通知回调（ATVV / HID）不是跑在主线程上，而是跑在 COM 的线程池线程上
——线程名长这样：`Dummy-3240`。这些线程**没有 asyncio 事件循环**。

而 v1.0.3 的 `_send_tx()` 是这么写的：

```python
asyncio.get_event_loop().create_task(_write())   # ❌ 在 Dummy 线程上直接抛异常
```

结果：**每一条 `MIC_OPEN` / `MIC_CLOSE` 都在投递阶段就失败**，日志里能看到：

```
MIC_OPEN schedule failed: There is no current event loop in thread 'Dummy-3240'
```

但紧接着又打了一句 `🎤 松手后自动重新开麦` —— **这是假成功**（只要命令"构造出来了"
就算成功，根本没管有没有发出去）。于是：

- 遥控器从来没收到过「我要常开麦」的指令 → 硬件只在你**按住的那一瞬**推流
- 松手 → `audio_stop` → 遥控器停止推流 → 混音显示「等待录音」
- 反复的 start/stop 把音频切得七零八落 → 识别端一直在等"这句话说完了吗" → 逐字往外冒

`session.py` 里还有 4 处同样的 `asyncio.get_event_loop()`（开麦/闭麦的延时定时器），
**一模一样的坑**，一并修掉。

### 修法

| 位置 | 原来 | 现在 |
|---|---|---|
| `main.py` 的 `_send_tx()` | `get_event_loop().create_task()` | `asyncio.run_coroutine_threadsafe(coro, _bridge_loop)` 回投主循环 |
| `main.py` 的 `_on_mic_open/close` | 无脑返回命令 | 发不出去就返回 `None`，不让 `mic_open_sent` 被置位 |
| `main.py` 的 `audio_stop` 分支 | 假成功日志 | 真没发出去就打 `❌ MIC_OPEN 未能发出` 的 ERROR |
| `session.py` 的 4 处定时器 | `asyncio.get_event_loop().call_later()` | `threading.Timer(daemon=True)` |

同时在 `run_bridge` 开头存一份主循环引用：

```python
_bridge_loop = asyncio.get_running_loop()
```

### 顺带修的

- `on_mic_open_result` 排新定时器前先取消旧的（原来会留一个僵尸定时器）
- `config.py` 新增 `VIRTUAL_TARGETS` 单一真源 —— `voice_ptt` 是虚拟目标不是键位组合，
  之前两个自检脚本各写死一份，导致 `check_keymap` 误报 FAIL（会挂 CI）
- `tools/smoke_console.py` 的默认触发键断言过期（默认已改成 `ctrl+win+shift`），同步更新

### 第二轮：外部代码审查中发现并确认的缺陷

拿到一份逐文件审查报告（8 条）。逐条读源码核实，**6 条成立、2 条站不住**，
成立的都修了：

#### 🔴 语音会话进行中不再做 GATT 读

CTL 特征既承载 `audio_start` / `audio_stop`，又被当成健康检查的读目标。
Windows 的 GATT 栈同一时刻只允许一个 ATT 事务，**读占着通道期间到达的通知会被压后甚至丢**。
一旦丢掉 `audio_stop`，`voice_active` 永远回不到 False → 下一次按语音键被当成
"第二次按下"直接收尾，用户看到的是「说着说着自己断了」。
触发条件极日常：按过任意键后 3 秒内就会来一次读（`key_check_window`）。

现在 `check_health()` 在 `streaming=True` 时直接跳过读、只查连接状态。
语音期间本来就有音频帧按 ~50Hz 刷新 `last_ble_activity`，这个读纯属多余。
`force=True`（watchdog 判定静默超时）时照读不误。

#### 🔴 确认键吞 Enter 的行为改为可关闭

`_on_key` 里 `return False` 会吞掉「语音会话进行中按下的确认键」。
但 Windows 低级键盘钩子**分不出遥控器和物理键盘**，所以物理键盘的 Enter 也被一起吞
—— 会话没结束前键盘回车是死的。

同时核实了 `keyboard` 库的真实机制（读源码，不是猜）：
`hook(cb, suppress=False)` 把回调挂进 `handlers`，而 `direct_callback` 只检查
`blocking_hooks` —— **suppress=False 时 `return False` 是空操作，一个键都拦不住**。
这就是"不开启拦截遥控器原生按键就会输入失败"的真正原因。

因为设备层无法区分，只能给逃生口：新增配置
`swallow_ok_during_voice`（默认 `true` = 维持原行为），控制台「设置 → 语音」可勾选。
关掉后确认键仍会结束语音会话，只是额外多打一个回车。

#### 🟠 `_find_cable` 多虚拟声卡时选错设备

原来是"第一个子串匹配"。装了 VB-CABLE + CABLE 2 + Voicemeeter 时，
`CABLE Input` 是 `CABLE 2 Input` 的子串，而 PortAudio 的设备顺序由驱动枚举决定，
可能挑中 `CABLE 2 Input` —— 微信那边选的却是 `CABLE Output`，表现为
「一点声音都没有」，日志上完全看不出选错了设备。

改为：全名相等 → 前缀 → 子串，三级优先。

#### 🟠 `_mute_loop` 异常时 BACKSPACE 可能卡在按下状态

原来 down/up 一起包在 try 里、异常直接 `pass`。若 `send_key_up` 抛错
（Interception 驱动被拔、权限被收回），异常被吞、循环继续 `send_key_down`；
用户在此时松手，循环会在"已按下未释放"的状态下退出 ——
之后每碰一下键盘都在连续删除。

改为：释放失败立刻 `break`，并在 `finally` 里补一次幂等释放。

#### 🟡 `tray_app._bridge_worker()` 丢掉 traceback

只 `print(f"[bridge] {e}")`，现场只剩一行光秃秃的消息，不知道哪一行炸的。
改为补 `traceback.print_exc()` —— 桥会自动重连，这条记录是事后唯一线索。

#### 🟡 v0.4 帧长不符时静默丢帧

`len(data) != caps.frame_size` 直接 `return None`，日志上看不出有帧被吞。
补了一行 `logger.debug`。

> ⚠ **刻意没有**采纳报告建议的 `abs(len - frame_size) <= 1` 容差：
> v0.4 每帧自带预测器/步长（`data[3:5]` / `data[5]`），长度对不上就是协议对不上，
> 硬解只会把噪声当语音喂给输入法。宁可丢帧也不要喂错。

#### 核实后判定**不成立**的两条

| 报告 | 实际 |
|---|---|
| `last_key_activity` 只在下键刷新，长按会误判掉线 | Windows 的按键自动重复本身就会持续产生 `down` 事件，一直刷新。且 watchdog 只多一次读，无实际影响 |
| `Config.load()` 缺配置迁移提示 | 本版已经加了 `CONFIG_VERSION` + `_migrate()`（见上节）。报告猜的"keymap 用数字 ID"从未存在过 |

### 验证

模拟一个 `Dummy-7777` 线程跑完整链路（能力协商 → 开麦 → 收流 → 闭麦 → 松手后补开麦）：

```
正常路径：sent == ['open', 'open']            ✅ PASS
故障路径（主循环已关）：sent == []，
  mic_open_sent 保持 False → main.py 打 ERROR  ✅ PASS
```

已固化为 `tools/check_ble_callback_thread.py`，随 `check_all.py` 进 CI。

---

## v1.0.3：语音键「按一下长输」+ 静音键「按住说话」+ 呆瓜化默认

### 背景：两种输入法语音方式，全部走输入法**原生**能力

微信输入法自己的「设置 → 语音输入」面板上就写着两种方式。本版不再自己造轮子，
而是**原样适配**这两种，并分别绑到遥控器的两个键上：

| 遥控器键位 | 转发给输入法的键 | 输入法侧的模式 | 行为 |
|---|---|---|---|
| **语音键**（麦克风图标） | 左Ctrl + 左Win + 左Shift | **启动语音输入**（切换式） | 按一下**开始**，再按一下（或按确认键）**结束** |
| **静音键** | Ctrl + Win | **按住说话**（PTT） | 按住说，松手即结束 |

> 设计原则：**输入法负责"怎么说话"，本程序负责"这段语音会话什么时候开始、什么时候结束"。**
> 状态机必须留在程序里 —— 因为只有程序知道「**松开遥控器按键 ≠ 语音说完了**」，
> 输入法自己是判断不了的。（这一条是开发中走过弯路后定下来的：一度把状态全甩给输入法，
> 结果是"松手就断"；也一度想自己合成"一直按住 Ctrl+Win"的假长按，那是重复造轮子。）

### 一、语音键：按一下就一直听着，松手不结束

v1.0.2 及以前只有「按住说话」，按住不放很累；设置里虽然有
「按一下开始，再按一下结束」这个选项，但**实际不生效**。两个根因：

| # | 根因 | 后果 | 修复 |
|---|---|---|---|
| 1 | `audio_stop`（遥控器上报"松手"）**无条件**调用了 `voice_hotkey_up()` | 一次按下 = 立刻发"开始"又发"结束"，自己把自己取消掉 → 点按模式永远不生效 | 松手**不再结束**语音会话；只有"程序认定的结束信号"才结束 |
| 2 | 遥控器只在按住期间推流，松手后 `stream_active=False`，音频帧被静默丢弃 | 就算输入法还在听，喂进去的也是静音 | 松手后自动补发 `MIC_OPEN` 并恢复 `stream_active`，**麦克风继续收音** |

结束语音会话的三种方式（任一即可）：

- 再按一次**语音键**
- 按**确认键**（OK / 中间那颗）
- 超过 **10 分钟**未结束 → 自动收尾（防止忘关导致一直占用麦克风）

> **防自激**：自动重开麦后，遥控器往往会回一个 `audio_start`。
> 这个信号**绝不能**被当成"用户第二次按下"（否则立刻又结束了）。
> 现在有 1.5 秒的宽限期，宽限期内的 `audio_start` 直接忽略。

### 二、静音键：按住说话（PTT）

新增映射目标 **`voice_ptt`**，「按键映射」页里给静音键选它即可，
对应组合键在 `config.voice_ptt_keys`（默认 `ctrl+win`）。
按下=发组合键按下，松手=发抬起，纯 PTT，不参与状态机。

### 三、呆瓜化：装上就用，不用做任何选择

不再让人一项项选。运行一次：

```bat
python tools\apply_voice_mode.py           :: 应用推荐配置（默认）
python tools\apply_voice_mode.py --show    :: 只看当前配置
python tools\apply_voice_mode.py --hold    :: 退回"全程按住说话"
```

推荐配置 = 输入法选**微信输入法**、语音触发方式选**按一下开始/再按一下结束**、
清空自定义触发键（用输入法原生键）、按键映射恢复默认（静音键=按住说话）、
开启系统麦克风参与混音、开启按键映射。**新装的默认配置就已经是这样了。**

### 四、按键匹配不再误伤（v1.0.2 留下的 bug）

HID 上报的按键名是"长名字"，旧匹配用的是**子串**匹配，导致物理键盘的键被遥控器规则误抓：

| 物理按键 | 旧行为 | 原因 |
|---|---|---|
| `Page Up` | 被当成**方向上键** | `"up"` 是 `"page up"` 的子串 |
| `Left Windows` | 被当成**方向左键** | `"left"` 是 `"left windows"` 的子串 |
| `Backspace` | 触发 **Esc** | `"back"` 是 `"backspace"` 的子串 |

现在：先查**精确匹配表**，兜底只对**多词键名**（如 `page up`、`left windows`）做
**词边界**匹配，单词键名不再参与兜底。回归用例 18 条，0 失败。

### 五、参考上游 vRemoter v1.1.1，作废一条旧结论

上游 [CHANGELOG](https://github.com/VincentKingHsu/vRemoter/blob/main/CHANGELOG.md)
明确写着：官方 Chromecast 遥控器支持**短按切换 + 长按说话**，且
"短按后遥控器麦克风却未持续收音"被列为**待修的 bug**。

> ❌ **作废本项目此前的一条错误结论**："遥控器麦克风只在按住时推流，软件改不了"。
> 那不是硬件限制，是**本程序漏发 `MIC_OPEN`** 造成的，本版已修。

**已知遗留（暂不解决）**：vRemoter 在 macOS 上能做到"只拦遥控器的键、放过物理键盘同名键"，
因为可以区分输入设备来源。Windows 的 `WH_KEYBOARD_LL` **拿不到设备来源**，
做不到按设备独占拦截 —— 「设置 → 拦截遥控器原生按键」打开后，物理键盘的同名键也会被一起吞掉。
要做到完全隔离需要装 Interception 驱动，与"开箱即用"冲突，暂缓。

### 六、工程防呆

- 新增 `config.VIRTUAL_TARGETS`：把"禁用 / 原样直通 / 语音键 / 按住说话"这类
  **不是组合键**的虚拟目标集中登记，自检脚本统一跳过。
  此前 `voice_ptt` 没登记，`check_keymap.py` 把它当组合键解析并报"键名无法解析" —— CI 会挂
- `tools/smoke_console.py` 的默认触发键断言同步更新为 `ctrl+win+shift`
  （= 微信输入法「启动语音输入」，默认档），并加了一条默认 `hotkey_mode == tap` 的断言
- 修正 `VOICE_HOTKEY_PRESETS` 里 `Ctrl+Win+Shift` 那档的说明：
  它原先写着"微信 PC 客户端"，但现在**默认就是它**（微信输入法的启动语音输入）

---

## v1.0.2：补齐界面（此前是「盲操」）+ 修掉持续 0 帧

v1.0.1 装到 HP 笔记本上以后暴露了两件事：**语音依旧一帧都收不到**，
以及**整个程序没有界面** —— 只能在托盘右键里点来点去，按键映射只能改 `config.json`
文件，等于盲操。本版针对这两点。

### 键位更正（重要）：微信输入法的「按住说话」是 `Ctrl+Win`

打开微信输入法自己的「设置 → 语音输入」，面板上写着两条，**和本仓库早前的说法完全相反**：

| 面板上的条目 | 键位 | 说明 |
|---|---|---|
| **按住说话** | **Ctrl + Win** | 按住可语音输入，松手结束 ← **本程序要的就是这条** |
| 启动语音输入 | 左Win + 左Ctrl + 左Shift | 按下即开始，按任意键结束（切换模式） |

所以：
- 默认触发键已经回到 **`ctrl+win`**（不是 `ralt`）。
- 早前"微信输入法默认长按右 Alt"是**错**的；**右 Alt 是豆包输入法的键**。
- 早前"`Ctrl+Win` 只在微信窗口内有效"也是**错**的（微信 PC 客户端那条同样是系统级）。
- **但不要再把键位当真理写死**：面板里是可以改的。拿不准就看面板，
  或跑 `python tools\test_voice_hotkey.py ctrl win` 实测一次。

> 讽刺的是：微信输入法这条 `Ctrl+Win`，正好撞上下一节那个「Win 会被系统改写成 `0xFC`」
> 的坑 —— 也就是说**修正键位之后，那个坑才真正变成必经之路**。
> 好在 `order_press()` 已经把 Win 排在最先按下，实测钩子能收到货真价实的 `vk=0x5B`。

### 一、持续 0 帧的真正原因：MIC_OPEN 从来没发出去

日志里一直是 `▶ Audio START` + `本次共收到 0 个音频帧`。对照上游客端源码后确认：

| | Chromecast 遥控器实际行为 | 本程序原来的假设 |
|---|---|---|
| 按语音键时上报的 ATVV 控制事件 | 只有 `AUDIO_START (0x04)` | 期待 `START_SEARCH (0x08)` |
| 结果 | 开麦命令 `MIC_OPEN` **一次都没发出去** | —— |

`MIC_OPEN` 原本只挂在 `start_search` 分支上，而这个遥控器**从不发** `start_search`
（上游客端 `BLEBridge.swift` 里也是同一个现象）。遥控器必须先收到 `MIC_OPEN`
才会真正上传麦克风数据 —— 所以它一帧都不推。

**修复**：`AUDIO_START` 到达时补发一次 `MIC_OPEN`（`SessionCoordinator.ensure_mic_open()`），
幂等、不重复开麦；并修正了 `mic_open_sent` 标记的生命周期 —— 它原先在会话关闭时
不复位，会导致**只有第一次能用、之后每次都 0 帧**。

> 顺带修掉一个潜伏的自激隐患：遥控器走 HID 键盘通道，与物理键盘在 Windows 上
> **无法区分**，键盘钩子既能看见用户按的键、也能看见程序自己注入的键。
> 映射目标一旦落回同一个键名（确认键 → Enter 就是典型）就会无限递归注入。
> 现在注入时会登记"这个键是我发的"，钩子识别到回声直接丢弃。

### 二、补上控制台与按键映射界面

对标 [vRemoter](https://github.com/VincentKingHsu/vRemoter) 的控制台：

- **音频页** —— **三路实时波形 + 分段电平表**（电脑麦克风 / 遥控器麦克风 / 混合输出，
  每路可静音、独奏、开关是否参与混合）、**增益滑块**（拖完立即生效，不用重启）、
  **混合输出设备下拉框**（自动列出所有输出设备，VB-CABLE 排最前）、**电脑麦克风选择**、
  输入法 / 触发方式（按住 / 点按）、**「测试语音键」按钮**（模拟长按 1 秒，
  直接验证输入法认不认这组键）
- **按键映射页** —— 每个遥控器按键一行，下拉框挑目标动作。目标集对齐 vRemoter 的
  `RemoteMappingTarget` 并换成 Windows 语义：方向上/下/左/右、回车、Esc、退格、
  Delete、Tab、空格、PgUp/PgDn、Home/End、系统音量±、静音、播放暂停、
  显示桌面、搜索、截图、任务管理器、复制/粘贴/撤销/全选…… 也可以
  **「录制任意按键…」** 自己按一个组合键进去。改动**即时保存、无需重启**。
- **设置页** —— 上半：音频（两路增益、混合输出设备、电脑麦克风）与语音
  （触发方式、触发键、拦截开关、开机启动）；下半：连接与关于（版本、配置目录）
- **日志页** —— 实时日志

> **关于「原样直通」**：遥控器走 HID 键盘通道，它自己的按键 Windows 本来就收得到。
> 所以映射目标里有一项 **`原样直通`**（默认用于方向键）＝ 不做任何额外动作。
> 改成其它动作时会「原生键 + 映射键」一起发出 —— Windows 无法单独拦掉遥控器原生键，
> 要完全接管需在「设置」页打开「拦截遥控器原生按键」（代价：物理键盘的同名键也会被吞）。

### 三、工程防呆

- 版本号在 `config.py` 与 `installer.iss` 两处，新增 `tools/check_version.py` 做一致性校验；
  CI 打 tag 时也会校验 tag 与 `APP_VERSION` 是否一致，不一致直接**构建失败**，
  避免"tag 是 1.0.3、装出来还写着 1.0.2"这种只能靠重新发版来修的事故
- 新增 `tools/smoke_console.py`：离屏把控制台真的建出来跑几轮刷新，
  控件名写错 / 变量漏定义这类问题在 CI 阶段就被拦下，而不是等用户点开才炸
- 新增 `tools/check_injection.py`：**真的把组合键按下去**，再用键盘钩子确认系统收到了。
  这是唯一能拦住「SendInput 静默失效」和「组合键顺序错」的一关 ——
  这两种故障的共同点是**日志里一切正常、输入法毫无反应**，肉眼查不出来。
  下面那个 Win 键的坑就是它抓出来的
- `tools/check_all.py` 一键跑完全部校验，输出 `ALL CHECKS PASSED` 即通过

#### 顺带查清的一个系统级坑：注入 `Ctrl+Win` 时 Win 会被换成 `0xFC`

排查「按住 `Ctrl+Win` 唤不起输入法」时发现，问题不在我们发得对不对，
而在**系统收到之后把内容改了**：

| 注入顺序 | 键盘钩子实际看到的事件 | 结果 |
|---|---|---|
| 先 Ctrl 再 Win | `vk=0xA2`、**`vk=0xFC`**（Win 被顶替掉了） | 输入法认不出 Win，组合键失效 |
| **先 Win 再 Ctrl**（现在的做法） | **`vk=0x5B` / `scan=0x5B`**、`vk=0xA2` | 钩子收到货真价实的 Win ✅ |

原因是 `Ctrl+Win` 属于 Windows 保留的**系统组合键**：Ctrl 已经按下时，
系统会把后到的 Win 改写成 `0xFC`，而且 Ctrl 与 Win **不会同时**出现在
`GetAsyncKeyState` 里。所以现在 `keys.py` 的按下顺序固定为 **Win 最先落下**
（`order_press()` 按修饰键优先级排序），`check_injection.py` 对带 Win 的组合
只认钩子事件、不拿 `GetAsyncKeyState` 当判据。

> 结论：微信输入法要的正是 `Ctrl+Win`，而它恰好是**最受系统限制**的那一组。
> 我们已经把合成按键能做到的做到最好（Win 最先落下），钩子确认收到真实 Win；
> 输入法到底认不认，请按 [使用教程](docs/使用教程.md) 第六步实测一次
> （控制台「音频」页也有「测试语音键」按钮）。
> 万一还是唤不起来，就把输入法的键位也改成**右 Alt**（一路不涉及 Win，最干净）。

---

---

## v1.0.1：真机实测抓到的四个问题

v1.0.0 发布后在真机上跑，**遥控器这一侧完全正常**（配对、ATVV 握手、MIC_OPEN、
遥控器推流全都对），但**按着说话输入法一个字都不出**。日志把范围锁死到了音频之后：

> ⚠️ **最容易搞混的一点：微信输入法 ≠ 微信 PC 客户端。**
>
> | 产品 | 按住说话的键 | 生效范围 |
> |---|---|---|
> | **微信输入法**（本程序对接的） | **Ctrl + Win** | **全局**——微信、豆包、记事本、浏览器都能用 |
> | **豆包输入法** | **右 Alt** | 全局 |
> | 微信 PC 客户端 4.1.8+ | `Ctrl+Win`（持续输入 `Ctrl+Win+Shift`） | 系统级，**不只**微信窗口内 |
>
> 以输入法自己的「设置 → 语音输入」面板为准，别抄文档。
>
> 🔴 **本节早前写的「微信输入法 = 右 Alt」和「Ctrl+Win 只在微信窗口内」两条都是错的**，
> 已在 v1.0.2 按输入法面板实测更正 —— 见下文「键位更正」。

| # | 问题 | 后果 | 修复 |
|---|------|------|------|
| 1 | 把「按住说话」做成了「点一下」，且键位写的是 `Alt+Shift+M` | 输入法的语音输入都是**长按说话、松开结束识别**，点一下只录到约 50ms 空气；`Alt+Shift` 还是 Windows 切换输入法的系统热键 | `keys.py` 改用 **SendInput** 的 key-down/key-up，语音开始=按住、结束=松开；`hotkey_mode` 支持 `hold`/`tap` |
| 2 | **每个采样被播放两遍**：`on_audio` 既直接塞进播放缓冲、又入队，而播放回调两条路都会取到同一批数据 | 音频变成断续、倍速的碎片，就算触发了输入法，喂给 ASR 的也是垃圾 | 删掉直接 extend，音频只走队列一条路 |
| 3 | watchdog 每 **180 秒**无条件重连：它以为「ATVV 静默 = 掉线」，但**遥控器按键走 HID 通道、不产生 ATVV 通知** | 每次重连约 4 秒内遥控器完全不可用 | ATVV 静默只当「疑似」，必须再做一次 GATT 读确认失败才断开；HID 按键也刷新活动时间 |
| 4 | 默认触发键一度被改成 `ralt`（右 Alt） | 微信输入法「按住说话」绑的其实是 **Ctrl+Win**，配成右 Alt 连输入法都唤不起来 —— 右 Alt 是**豆包输入法**的键 | 键位回到 `ctrl+win`（v1.0.2 按输入法面板再次更正，见下）。这一轮留下的**真正有用的机制经验**是：修饰键必须用 `KEYEVENTF_SCANCODE` 下发**硬件扫描码**，否则左右分体的键会被当成左边那只；而 Win 组合另有一条系统限制，见 v1.0.2 那一节 |

同时补齐了**音频可见性**（此前完全没有）：控制台现在有
**实时语音波形 + 电平 + 本次帧数/峰值 + 「最后音频 N 秒前」**，
`Audio STOP` 时日志会写明本次收到多少帧。

> 这一整套的意义：把「没收到音频」和「收到了但没触发输入法」**一刀切开**，
> 不用再靠猜。控制台里出现「⚠ 本次 0 帧」就是前者。

---
